package scanner

// Explanantion: 발견이 읽는 사람에게 필요한 두 가지.
type Explanation struct {
	Why string // 왜 위험한지
	Fix string // 어떻게 고치는지
}

// Explain(): 규칙 코드의 설명
func Explain(code string) (Explanation, bool) {
	e, ok := explanations[code]
	return e, ok
}

var explanations = map[string]Explanation{
	"base-href-external": {
		Why: "<base> 태그는 페이지 안의 모든 상대 주소가 기준으로 삼는 주소입니다. " +
			"이 한 줄이 바뀌면 본문을 하나도 건드리지 않고 스크립트와 폼 전송지가 통째로 다른 서버로 넘어갑니다.",
		Fix: "<base> 를 쓸 이유가 없다면 지웁니다. 필요하다면 같은 출처의 주소만 적고, " +
			"CSP 에 base-uri 'self' 를 두어 나중에 끼어드는 <base> 를 브라우저가 막게 합니다.",
	},
	"cross-origin-password-form": {
		Why: "비밀번호를 받는 폼이 이 페이지와 다른 도메인으로 전송합니다. 화면은 이 사이트인데 입력값은 남의 서버로 갑니다 — " +
			"피싱 페이지에서 가장 흔한 모양이고, 정상 사이트가 이렇게 할 이유는 거의 없습니다.",
		Fix: "action 이 가리키는 도메인을 확인합니다. 외부 인증 서비스를 쓴다면 그 서비스의 페이지로 보내는 방식이 맞고, " +
			"우리 페이지에서 비밀번호를 받아 넘기지 않습니다.",
	},
	"form-action-ip": {
		Why: "폼이 도메인 이름이 아니라 IP 주소로 직접 전송합니다. 정상 서비스는 이름과 인증서를 씁니다 — " +
			"IP 로 받는 것은 급히 세운 수집 서버이거나 이름을 남기고 싶지 않은 쪽의 특징입니다.",
		Fix: "전송지를 확인합니다. 의도한 것이 아니면 이 폼은 심어진 것입니다. " +
			"의도한 내부 시스템이라면 도메인과 인증서를 붙여 https 로 바꿉니다.",
	},
	"exfil-channel": {
		Why: "페이지가 외부 메시징 서비스의 API 주소로 값을 보냅니다. 공격자가 훔친 정보를 자기 서버 없이 받아 가는 통로입니다 — " +
			"봇 하나면 되고, 그 도메인은 정상 서비스라 차단하기도 어렵습니다.",
		Fix: "그 전송이 의도한 기능인지 확인합니다. 아니라면 해당 코드가 어떻게 들어왔는지 추적해야 합니다. " +
			"CSP 의 connect-src 와 form-action 으로 나갈 수 있는 곳을 제한합니다.",
	},
	"data-uri-document": {
		Why: "iframe 이 주소가 아니라 data: 로 문서 전체를 품고 있습니다. 서버에 흔적이 남지 않고, " +
			"주소 목록으로 막는 방어도 지나갑니다. 공격자가 페이지 안에 자기 문서를 통째로 심는 수법입니다.",
		Fix: "문서를 일반 주소로 옮기고 iframe 의 src 를 그 주소로 바꿉니다. " +
			"CSP 에 frame-src 를 두어 data: 를 막아 두면 나중에 끼어드는 것도 함께 막힙니다.",
	},
	"meta-refresh-scheme": {
		Why: "meta refresh 는 보통 다른 페이지로 넘길 때 씁니다. 그런데 넘길 곳이 일반 주소가 아니라 data: 나 javascript: 입니다 — " +
			"방문자가 아무것도 누르지 않아도, 스크립트 태그 하나 없이 코드나 문서가 실행됩니다.",
		Fix: "이동이 필요하면 서버에서 3xx 리다이렉트를 쓰거나, meta refresh 의 대상을 http·https 주소로만 둡니다.",
	},
	"local-system-object": {
		Why: "스크립트가 WScript.Shell 이나 FileSystemObject 처럼 방문자 PC 의 파일과 프로세스를 다루는 객체를 만듭니다. " +
			"웹 페이지가 할 일이 아니며, 악성코드를 내려받아 실행시키는 드로퍼의 전형입니다.",
		Fix: "정상 페이지에 있을 코드가 아닙니다. 서버가 침해됐는지 먼저 확인하고 해당 스크립트를 제거합니다.",
	},
	"encoded-shellcode": {
		Why: "스크립트 안에 %u 로 적힌 긴 값이 있는데, 풀어 보면 글자가 아니라 이진 코드입니다. " +
			"취약점 공격 도구가 셸코드를 문자열로 위장해 숨기는 전형적인 모양입니다.",
		Fix: "정상 코드가 아닐 가능성이 높습니다. 서버 파일의 변경 시각과 접속 기록으로 언제 들어왔는지 확인하고 제거합니다.",
	},
	"webshell-signature": {
		Why: "알려진 웹셸 관리 화면의 이름이 페이지에 보이고, 파일을 올릴 수 있는 칸이 함께 있습니다. " +
			"웹셸은 침입자가 서버에 심어 두고 파일과 명령을 다루는 화면이라, 이미 뚫렸다는 신호입니다.",
		Fix: "그 파일을 찾아 제거하고, 언제 어떤 경로로 올라왔는지 접속 기록을 확인합니다. " +
			"비밀번호와 키는 전부 교체하는 편이 안전합니다.",
	},
	"mixed-script-host": {
		Why: "호스트 이름의 한 라벨 안에 라틴·키릴·그리스 문자가 섞여 있습니다. 눈으로는 같아 보이지만 다른 도메인입니다 — " +
			"유명 사이트의 이름을 흉내 내는 위조(호모그래프)에 쓰입니다.",
		Fix: "그 주소가 의도한 곳인지 한 글자씩 확인합니다. 쓰는 도메인을 고정해 두고 CSP 로 허용 출처를 제한합니다.",
	},
	"phishing-interstitial": {
		Why: "받아 온 것이 사이트의 내용이 아니라 Cloudflare 가 보여 주는 피싱 경고 화면입니다. " +
			"제3자가 이 주소를 피싱으로 분류했다는 뜻이며, 우리 스캐너의 판정이 아니라 남의 판정입니다.",
		Fix: "이 주소는 열지 않는 편이 좋습니다. 내 사이트가 이렇게 분류됐다면 침해 여부를 확인하고 Cloudflare 에 재심사를 요청합니다.",
	},
	"cleartext-credentials": {
		Why: "로그인 폼이 http:// 로 전송됩니다. 같은 네트워크에 있는 누구나 — 공용 와이파이, 중간 공유기, 경로의 장비 — " +
			"비밀번호를 그대로 읽을 수 있습니다. 페이지가 https 여도 보내는 곳이 http 면 소용이 없습니다.",
		Fix: "폼의 action 을 https:// 로 바꿉니다. 인증서가 없다면 발급이 먼저이고, 그 전까지 로그인 기능을 여는 것은 위험합니다.",
	},
	"local-credential-post": {
		Why: "비밀번호 폼이 localhost 나 127.0.0.1 로 전송됩니다. 방문자 자신의 PC 에서 도는 프로그램으로 값이 가는 것이라, " +
			"개발 중 설정이 그대로 배포됐거나 로컬에서 값을 가로채려는 것입니다.",
		Fix: "배포 설정에서 전송지를 실제 서버 주소로 바꿉니다. 개발용 값이 배포에 섞이지 않도록 빌드 단계에서 갈라 둡니다.",
	},
	"weak-password-field": {
		Why: "비밀번호로 쓰이는 입력 칸의 type 이 password 가 아닙니다. 화면에 글자가 그대로 보이고, " +
			"브라우저의 비밀번호 관리와 자동완성 보호도 동작하지 않습니다.",
		Fix: "그 입력 칸의 type 을 password 로 바꿉니다. 보기·숨기기 기능이 필요하면 버튼으로 type 을 전환합니다.",
	},
	"javascript-url": {
		Why: "링크나 속성의 주소가 javascript: 로 시작합니다. 주소가 와야 할 자리에 코드가 있는 것이고, " +
			"그 값이 사용자 입력에서 오면 그대로 XSS 가 됩니다.",
		Fix: "href=\"javascript:…\" 대신 button 과 이벤트 처리기를 씁니다. " +
			"링크 모양이 필요하면 href 에 실제 주소를 두고 클릭에서 기본 동작을 막습니다.",
	},
	"iframe-sandbox-escape": {
		Why: "sandbox 에 allow-scripts 와 allow-same-origin 을 함께 주면, 그 안의 스크립트가 자기 sandbox 속성을 지울 수 있습니다. " +
			"가두려고 쓴 장치가 사실상 없는 것이 됩니다.",
		Fix: "둘 중 하나를 뺍니다. 외부 콘텐츠라면 allow-same-origin 을 빼는 쪽이 맞습니다.",
	},
	"dangerous-download": {
		Why: "링크가 가리키는 것이 문서가 아니라 실행되는 파일입니다. .hta 는 브라우저 밖에서 시스템 권한으로 도는 스크립트라, " +
			"내려받아 여는 순간 방문자 PC 에서 코드가 실행됩니다.",
		Fix: "그 링크가 의도한 것인지 확인합니다. 파일 배포가 목적이라면 무엇을 받는 것인지 링크 옆에 밝힙니다.",
	},
	"noscript-breakout": {
		Why: "noscript 안의 내용이 속성값에 숨긴 </noscript> 로 태그를 빠져나갑니다. 스크립트가 켜진 브라우저는 " +
			"noscript 안을 글자로 보는데, 이렇게 끊기면 그 뒤가 진짜 태그로 해석됩니다 — 필터를 지나가는 수법입니다.",
		Fix: "noscript 안에 사용자 입력을 그대로 넣지 않습니다. 넣어야 한다면 꺾쇠와 슬래시를 이스케이프합니다.",
	},
	"obfuscated-eval": {
		Why: "eval() 의 인자 안에서 atob 같은 디코더가 함께 쓰입니다. 실행할 코드를 눈에 안 보이게 만들어 두었다는 뜻이고, " +
			"정상 코드가 굳이 이렇게 할 이유는 드뭅니다.",
		Fix: "무엇을 실행하는지 풀어 확인합니다. 의도한 코드라면 eval 없이 직접 쓰고, 모르는 코드라면 어떻게 들어왔는지 추적합니다.",
	},
	"mixed-content": {
		Why: "https 페이지가 http:// 리소스를 불러옵니다. 그 요청은 암호화되지 않아 경로에 있는 누구나 내용을 보거나 바꿔칠 수 있고, " +
			"스크립트라면 페이지 전체를 넘겨주게 됩니다. 요즘 브라우저는 대개 차단합니다.",
		Fix: "주소를 https:// 로 바꿉니다. 대상이 https 를 지원하지 않으면 그 파일을 우리 서버로 옮겨 제공합니다.",
	},
	"resource-ip-literal": {
		Why: "스크립트나 이미지 같은 하위 리소스를 도메인이 아니라 IP 주소에서 불러옵니다. 인증서가 이름과 맞물리지 않고, " +
			"주소가 바뀌면 조용히 깨지며, 정식 배포망이 아닌 곳을 가리키는 경우가 많습니다.",
		Fix: "도메인 이름으로 바꾸고 https 로 제공합니다. 내부망 자원이라면 외부에서 보는 페이지에 넣지 않습니다.",
	},
	"inline-handler": {
		Why: "onclick 같은 속성에 코드가 직접 적혀 있습니다. 그 자체가 취약점은 아니지만, 이런 속성이 하나라도 있으면 " +
			"CSP 에서 script-src 'unsafe-inline' 을 뺄 수 없습니다 — XSS 방어의 핵심 장치를 켜지 못하게 됩니다.",
		Fix: "코드를 별도 .js 파일로 옮기고 addEventListener 로 붙입니다. 전부 옮기면 CSP 에서 'unsafe-inline' 을 뺄 수 있습니다.",
	},
	"sri-missing": {
		Why: "외부에서 불러오는 스크립트나 스타일에 integrity 가 없습니다. 그 파일이 바뀌면 우리 페이지에서 바뀐 코드가 그대로 돕니다. " +
			"다만 대부분의 사이트가 이 상태라, 이것만으로 위험을 말하기는 어렵습니다.",
		Fix: "주소를 버전까지 고정하고 integrity 해시와 crossorigin 을 붙입니다. " +
			"버전을 고정할 수 없는 주소라면 파일을 우리 서버로 가져와 제공하는 편이 확실합니다.",
	},
	"zero-width": {
		Why: "눈에 보이지 않는 제로폭 문자가 단어 사이에 끼어 있습니다. 사람에게는 같은 글자로 보이지만 문자열 비교에서는 다른 값이라, " +
			"필터나 검색을 피하는 데 쓰일 수 있습니다. 조판이나 이모지에서 정상적으로 쓰이기도 합니다.",
		Fix: "그 자리에 제로폭 문자가 필요한지 확인합니다. 필요 없으면 지우고, 사용자 입력을 받는 곳이라면 저장 전에 걸러냅니다.",
	},
	"target-blank-no-rel": {
		Why: "target=_blank 로 여는 링크에 rel=noopener 가 없습니다. 예전 브라우저에서는 새로 열린 페이지가 " +
			"window.opener 로 원래 탭의 주소를 바꿀 수 있었습니다. 요즘 브라우저는 기본으로 막으므로 실제 위험은 낮습니다.",
		Fix: "rel=\"noopener noreferrer\" 를 붙입니다. 옛 브라우저를 고려하지 않는다면 그대로 두어도 됩니다.",
	},
}
